# PROJECT_MEMORY.md
> **Project Name:** kurandakimesaj (Kur'an'da ki Mesaj)
> **Last Updated:** 2026-08-04
> **Current Phase:** Sprint 1-4 + cihaz QA + Backend + Tasarım/Mimari + **Büyük Sprint (2026-06-20)** + **H4-H10 Sprint (2026-06-20)** + **Ayet/Sure Tanıma Sprint (2026-06-23)** TAMAMLANDI. **Ayet Tanıma:** sesli tilavet (Groq Whisper) + Instagram görseli (Gemini Flash OCR) → in-memory QuranMatcher → yetkili sure:ayet+meal; tam Kur'an asset 6236 ayet; drift schemaVersion→4; `dart analyze` 0 hata, 26/26 test. Bekleyen: `supabase functions deploy quran-vision/quran-recognize` + cihaz E2E. `dart analyze` 0 sorun. **Büyük Sprint:** PRO gate kaldırıldı (7 dosya), şablon galerisi silindi, mucizeler 10→30 / kıssalar 8→20 / rüya 20→54, zekat TCMB+Hanafi/Şafi, Canva benzeri video editör (image_picker+RepaintBoundary+Storage), sesli AI asistan (speech_to_text+duygu haritası+ana ekran FAB), feed görsel/video yükleme, namaz takip günlüğü, okuma hedefi+streak, 9:16/1:1 story toggle. **H4-H10 Sprint:** Sesli tefsir (flutter_tts), Sure Quiz (SurahQuizScreen), dua hatırlatıcısı+İslami takvim bildirimleri (NotificationService), Kur'an istatistikleri (StatsService+ProgressScreen), favori sure (FavoritesService+yıldız UI), kullanıcı profil sayfası (UserProfileScreen+/profile/:id). Drift schemaVersion→3 (ReadingEvents+FavoriteSurahs). **Manuel bekleyen:** Supabase Dashboard'da `docs/supabase/follows_migration.sql` çalıştır (takip özelliği için). **Tasarım Borcu Kapanışı (2026-06-25):** kalan P2/P3 tasarım borçları temizlendi — onboarding "aha" (3→4 adım, ilgi alanına göre küratörlü ilk ayet kartı + "İlk videonu oluştur" CTA→stüdyo), sohbet baloncuk birleştirme (`AppRadii.chatBubble`/`bubble`16/`bubbleTail`4 token + 4 regresyon testi), Home hızlı-işlem gerekçe doc-comment'i, kök `DESIGN.md` (token tabloları). Render-retry ölü-kod gerekçesiyle ertelendi (kuyruğu besleyen producer yok). `dart analyze` temiz, `flutter test` 32/32. **Ana Ekran Widget'ları Sprint (2026-06-30):** telefon ana ekran widget'ları (Android App Widget + iOS WidgetKit) **ezan saati** + **günün ayeti** + uygulama içi yeni kartlar (hicri tarih, kıble/cami kısayolu). `home_widget` köprüsü, `WidgetSyncService` veri yazımı, deep-link (homeWidget://prayer|ayah → /prayer|/daily-ayah), `workmanager` ile Android periyodik yenileme. `dart analyze` 0, 18/18 test, Android debug APK derlendi. **iOS Faz 3/4 macOS-Xcode manuel adımları bekliyor** (`ios/KuranWidget/SETUP.md`).
> **Active Context (2026-08-04 — BUILD 4 ASC'DE, REDDE YANIT + EKRAN KAYDI GÖNDERİLDİ, SÜRÜM YENİDEN İNCELEMEDE):** Bir önceki girişteki "KALAN" üç maddesi de kapandı. **Codemagic:** `iOS App Store` iş akışı `main` / commit **`b210eb3`** üzerinden çalıştırıldı (build index 4, ID `6a710f3cc529f72d91069095`, Mac mini M2, **6m54s**, durum `finished` — bu kez post-processing hatası YOK), imzalı `kurandakimesaj.ipa` 30.49 MB. **ASC'de Version 1.0.0, Build (4) `Complete`** (4 Ağu 01:06). Dikkat: `pubspec.yaml` `1.0.1+4` olmasına rağmen yüklenen binary **1.0.0 (4)** olarak göründü — build-name override'ı sayesinde inflight **1.0.0** sürümüne doğrudan atanabildi (1.0.1 olsaydı sürüm numarasını da değiştirmek gerekirdi). **ASC işlemleri (use-my-browser):** sürümden **eski Build 2 çıkarılıp Build 4 atandı**; **App Review Notes İngilizce olarak Guideline 1.2'nin beş maddesine göre yeniden yazıldı** (kayıt/giriş ÖNCESİ zorunlu EULA onayı → `home_screens.dart`; `pending/approved/rejected` ile filtreleme; "Bildir" → `reportPost/reportComment`; "Kullanıcı engelle" + Ayarlar → Engellenen Kullanıcılar → `blockUser`/`filterBlockedFeedPosts`; 24 saat taahhüdü → `moderation_screen.dart` Bekleyen/Şikayetler sekmeleri; demo hesap `test@gmail.com`/`554455`), Save → disabled = kaydedildi. **Redde yanıt** Resolution Center'da yazıldı (2690 karakter, her madde için kod karşılığı + ekran yolu) ve **fiziksel cihazda çekilen `apple review.mp4` (2.6 MB) eklenip gönderildi; ardından "Resubmit to App Review" yapıldı** (dosya seçimi + son iki tık kullanıcı tarafından). **Yeni doğrulanmış ASC davranışları (site-pattern'e de yazıldı):** (a) Resolution Center'ın **"Attach File" alanı programatik yüklemeyi kabul ETMİYOR** — `upload_file` hem butona hem görünür kılınmış gizli input'a "File uploaded" döner ama `input.files.length` **0** kalır (React alanı sıfırlıyor); ek dosyayı **kullanıcı elle seçmek zorunda**. (b) Reply metni **"Save Draft"** ile saklanır, **"Continue Draft"** ile aynen geri açılır — uzun metni yazıp dosya beklerken kaybetmemenin yolu bu. (c) Build satırındaki **Delete** düğmesi a11y'de "interactive değil" hatası verir; `row.querySelector('button').click()` çalışır, ardından **"Add Build"** modalı açılır. (d) `Resubmit to App Review` düğmesi metadata/build değişikliğinden sonra bile `disabled` görünebilir; asıl akış redde **yanıt gönderdikten sonra** açılır. **Not:** gönderim ekran doğrulaması yapılamadı (tarayıcı yeniden başlayınca ASC oturumu düştü); gönderildi bilgisi kullanıcı beyanına dayanıyor.
> **Active Context (2026-08-04 — 9 MADDELİK PLANIN 3 FAZI DA UYGULANDI; BUILD 3 ASC'DE AMA BOZUK, GÖNDERİLMEMELİ):** 1.0.0 **reddedildi** (gönderim `cf08e6a4-b956-46b3-9064-2ecc40fa5742`, 24 Tem, **Guideline 1.2 — UGC**: EULA onayı, içerik filtreleme, bildirme, engelleme, 24 saat içinde işlem). 6 fazlı plan (`update1.0.1update.md`) uygulandı. **Moderasyon kapısı UI'da değil VERİTABANINDA:** `feed_posts.status` (`pending`/`approved`/`rejected`) + RLS (`feed_select_visible`/`feed_select_admin`/`feed_update_admin`) + `feed_posts_moderation_guard` BEFORE INSERT/UPDATE trigger'ı (SECURITY DEFINER, pinned search_path). Trigger; INSERT'te `status`'ü zorla `pending` yapar, kullanıcının kendi içeriğini onaylamasını 42501 ile reddeder, **onay sonrası içerik değişikliğini tekrar kuyruğa atar** (bait-and-switch) ve `media_url`/`video_url`/`thumbnail_url`'ün yalnız kullanıcının kendi `post-media/{uid}/` klasörünü göstermesini zorlar (dış host → izleyici IP sızıntısı kapatıldı). Kolon ayrıcalığı YETMEZ çünkü admin de `authenticated` rolündedir — kök çözüm trigger. Yorumlar da kapsamda (`comments_select_visible` + `comments_delete_admin`). **3 migration üretimde:** `20260725120000_moderation_queue` (+`profiles.is_admin`, `follows` çift politika temizliği), `20260725130000_reels_kind` (`kind` → yalnız `video`/`still`; CHECK ile birlikte `default 'ayah'`→`'still'` de değişmeli, yoksa `kind`'siz her insert patlar), `20260725140000_post_media_limits` (bucket 20MB + MIME allowlist + `post_media_delete_admin`). Mevcut kayıtlar **bilerek** toplu onaylanmadı (uygulama zaten moderasyonsuzluk yüzünden reddedildi; ayrıca `feed_posts` boş çıktı). **Adversaryal denetimlerde 11 gerçek açık bulundu** (hiçbiri `flutter analyze`/testlerde görünmüyordu), aralarında `ayah-finder/index.ts`'te korumasız ikinci `fetch` (yerel sunuculardan içerik çekilerek **ampirik olarak sömürüldü**, `downloadImage` silindi), Gemini anahtarının query string'de olması (→ `x-goog-api-key` başlığı; ağ hatası mesajı URL'yi taşıyordu), stüdyonun ikinci yayınlama yolunun telif onayını atlaması, video controller kimlik yarışı, sınırsız video yükleme. **Kalan bilinçli risk:** `link_resolver.ts`'te DNS-rebinding TOCTOU (kör SSRF + `image/*` kapısı; `ponytail:` yorumu + `console.warn` ile işaretli). **Dış yapılandırma (use-my-browser ile):** `terms.html` GitHub Pages'te canlı (sıfır tolerans maddesi zorunlu); Supabase Apple provider **etkin** — native akışta **yalnız Client IDs = `com.kurandakimesaj.app` yeter, Services ID/.p8 GEREKMEZ** (o web/OAuth akışının gereği); Apple Developer'da App ID `C497C57RH2`'ye **Sign In with Apple** eklendi (Save → "Modify App Capabilities" onayı iki adımlı), bu profili Invalid yaptı → `KDRAQ8GKZ3` Edit→Save ile yeniden üretildi (Active); **Codemagic'te SAKLANAN profil silinip yeniden çekildi** — `ios_signing` her build'de Apple'dan çekmez, Apple'da yenilemek tek başına yetmez; 3 hesap (`yiit55400@gmail.com`, `test@gmail.com`, `erenasanpp@icloud.com`) `is_admin=true`. Moderasyon kapısı üretimde geçici bir kullanıcıyla **uçtan uca kanıtlandı** (3 saldırı da bloklandı, kullanıcı silindi). **`GOOGLE_IOS_CLIENT_ID` bilerek tanımsız** (Cloud Console'a bot koruması yüzünden girilemedi): iOS'ta `GoogleSignIn.initialize` web ID'ye ek olarak iOS client ID ister; `hasGoogleSignIn` yalnız web ID'ye baktığı için buton görünüp her dokunuşta patlıyordu → `AuthScreen` artık iOS'ta bu değişken yoksa Google butonunu hiç göstermiyor (commit `455ecd3`). **Build 3 (`release/1.0.1`) ASC'ye yüklendi (UPLOAD SUCCEEDED, 1.0.1/3) AMA BOZUK** — alt menü çubuğu tüm ekranı kaplıyor, ana ekran boş görünüyor (bkz. Karar Günlüğü `Center` tuzağı). Düzeltme + regresyon testi commit `b7cc67c`; cihazda (SM S731B) ekran görüntüsüyle doğrulandı. `flutter analyze` 0, **flutter test 154/154**, `deno test` 27/27. **FAZ A (2026-08-03, build 4 hazırlığı) UYGULANDI:** cihaz kullanımında bulunan 9 sorundan **4'ü** düzeltildi (plan `g-rev-bir-implantation-glimmering-dream.md`, skill/ajan eşlemesi `SKILL.md`) — sıfır migration, sıfır native, sıfır yeni paket. **Madde 9 (en kritik):** "Reels'e Yayınla" hiç çalışmıyordu (sliver-paint tuzağı, bkz. Karar Günlüğü); önizleme kaydırılan listeden çıkarılıp sabit 360×640 `FittedBox` tuvale taşındı, `_renderPng()` başına `endOfFrame` eklendi, `.animate().fadeIn()` silindi. **Madde 8:** profil ızgarası boş kutular gösteriyordu → `getUserPosts` `select('*')` + `FeedPost.fromMap`, hata yutma kaldırıldı, "İncelemede" rozeti + "Tekrar dene" eklendi. **Madde 3:** soğuk başlatmada paylaşım kayboluyordu → `_onShared` `/splash`teyken push etmiyor, rotayı `SplashScreen._go` üstleniyor. **Madde 7-kısmi:** Mesajlar boş-durum metni dürüstleştirildi + ulaşılamayan "çevrimdışı sohbet" dalı silindi (`_offlineMessages`, `_sendOffline`, `conversationId` artık `required String`). **Doğrulama:** `dart analyze` 0, **flutter test 136/136** (taban 127 + 9 yeni); üç yeni regresyon testi de düzeltme geçici geri alınarak **kırmızıya döndüğü GÖRÜLDÜ** (ders #10). **FAZ B ve FAZ C DE UYGULANDI (2026-08-03/04, kullanıcı "bütün fazları bitir" dediği için — plan bunları Apple onayı SONRASINA koymuştu).** **Faz B:** madde 2 arka plan sesi (`just_audio_background 0.0.1-beta.17`, TAM sürüme pinli; `JustAudioBackground.init` bootstrap'ta `runApp` öncesi; `AudioService.playUrl(url, {required title})` → `setAudioSource(AudioSource.uri(tag: MediaItem(...)))` — `setUrl` metadata taşımaz; `quran_screens.dart` `dispose`taki `_player.stop()` SİLİNDİ; TTS ses odağı `TtsService(onSpeakStart:)`; manifest'e `xmlns:tools` + `WAKE_LOCK`/`FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_MEDIA_PLAYBACK` + `AudioService` service + `MediaButtonReceiver`; `MainActivity : AudioServiceActivity()` — manifest'teki activity ADI korundu çünkü widget/deep-link/paylaşım filtreleri ona bağlı; iOS `UIBackgroundModes: [audio]`), madde 1 (Ana Sayfa FAB + **`heroTag: 'home_ayah_finder_fab'`** — `FeedScreen` FAB'ı tag'siz ve `StatefulShellRoute` iki dalı canlı tutuyor), madde 4 (`FeedPost.caption` artık OKUNUYOR + `ReelCaption` genişletme + `_ReelComposition` latent taşma). **Faz C:** madde 7-tam (RPC'siz `openDirectConversation` — sıra ZORUNLU, id İSTEMCİDE üretilir çünkü insert sonrası `.select()` 0 satır döner; profilde "Mesaj Gönder", `ChatScreen`de "Kullanıcıyı engelle"), madde 6 (`pro_image_editor 13.3.0`; `kStudioEditorConfigs` → `OutputFormat.png` + sticker/emoji/audio KAPALI), madde 5 (küratörlü müzik: FK kürasyon kapısı, ikinci `AudioService` örneği, stüdyoda "Müzik" şeridi). **Doğrulama:** `dart analyze` 0, **flutter test 154/154**, `flutter build apk --debug` ✓ (her iki yeni paketten sonra ayrı ayrı), `receive_sharing_intent 1.8.1` pini İKİ pakette de kırılmadı. **CİHAZDA DOĞRULANDI (SM S731B, Android 16 / API 36, 2026-08-04):** uygulama `flutter run` ile kuruldu ve açıldı, başlangıçtan itibaren **0 `E/flutter`/`Exception`/`FATAL`**. Madde 2 sistemden okunarak kanıtlandı: medya bildirimi `android.title="Bakara, 2. ayet"` (birim testin kilitlediği `mediaTitleFor()` biçimi), `category=transport` + `flags=ONGOING_EVENT|NO_CLEAR` (→ `androidNotificationOngoing` etkili), `com.ryanheise.audioservice.AudioService` foreground service `MediaBrowserService` intent'iyle AYAKTA ve **API 36'da SecurityException YOK** → `FOREGROUND_SERVICE_MEDIA_PLAYBACK` izni doğru. Logda yalnız `W/MessageQueue: MediaCodec$EventHandler ... dead thread` uyarıları (video decoder dispose gürültüsü, çökme yok). **ÜÇ MIGRATION YAZILDI AMA UYGULANMADI** (`20260803120000_reel_audio`, `20260803120100_reel_audio_bucket`, `20260803120200_dm_block_gate`) — Supabase erişimi yoktu. Gönderim öncesi ZORUNLU: (a) engellemenin TERS yönü (security definer olmasaydı yanlışlıkla geçerdi), (b) rastgele `audio_track_id` insert'i **23503** ile reddedilmeli. Müzik şeridi migration uygulanana kadar cihazda GÖRÜNMEZ (kütüphane boş → şerit gizli, bilinçli). **GÖNDERİM SIRASI ÖNERİSİ DEĞİŞMEDİ: Faz A'yı AYRI commit olarak build 4'e gönder**, B+C'yi 1.0.2'ye bırak — madde 2 yeni arka plan yeteneği, madde 6 yeni düzenleme yüzeyi açıyor; redde yanıt turunda ikinci inceleme konusu yaratırlar. **`StudioScreen` → Notifier refactor'ü YAPILMADI** (gerekçe `tasks/todo.md`'de). **iOS build doğrulanmadı** — `flutter build ios --no-codesign` macOS ister, bu makine Windows; Codemagic'te bakılmalı. **KALAN:** (1) `release/1.0.1`'den yeni Codemagic build'i (build 4) — profil/değişkenler hazır, önceki build ~5dk sürdü; (2) fiziksel cihazda ekran kaydı (senaryo `docs/APP_REVIEW_RESPONSE.md` §3); (3) ASC'de redde yanıt (İngilizce metin §1'de hazır) + demo hesabı `is_admin` olmalı yoksa inceleyici moderasyon ekranını göremez. PR #1 açık (`release/1.0.1` → `main`), henüz birleştirilmedi.
> **Active Context (2026-07-23 — İLK APP STORE DEPLOY BAŞARILI, IPA ASC'de Processing):** Kullanıcı "ASC'yi son kez kontrol et, uygunsa Codemagic'ten deploy başlat, Supabase çalışıyor mu bak" dedi. **Supabase DOĞRULANDI (çalışıyor):** `env.json`'daki canlı değerlerle (`SUPABASE_URL=https://uytqxgcbohdlwwxemgyy.supabase.co`, anon key 2036'ya kadar geçerli) — Auth health 200, REST `profiles` gerçek kayıt döndürdü 200, **App Review demo hesabı `test@gmail.com`/`554455` signInWithPassword → HTTP 200** (STORE_SUBMISSION.md'deki "demo hesap 21 Tem HTTP 400" notu ARTIK GEÇERSİZ, hesap 2026-07-07 oluşturulmuş+onaylı). **ASC (use-my-browser ile):** inflight 1.0.0 metadata TAM — 6/10 ekran görüntüsü (6.5" Display) yüklü, tanıtım/açıklama/anahtar kelime/telif kayıtlı (Save disabled), demo hesap+iletişim(+90...)+notlar dolu; **tek eksik: yüklü build**. **İki Codemagic engeli çözüldü:** (1) **provisioning profile** — Codemagic'te yalnız `com.deyiver.app` vardı; Apple Developer Portal'da `com.kurandakimesaj.app` için App Store profili oluşturuldu (App ID zaten kayıtlı, cert = "Eren Asan (Distribution)" exp 2027-05-02 = Codemagic'teki `deyiver-distribution`) → Codemagic Fetch profiles ile `kurandakimesaj-distribution` olarak çekildi; (2) **`kdm_runtime` env grubu** — kişisel hesaplarda global gruplar kaldırıldığı için **APP seviyesinde** oluşturuldu, `SUPABASE_URL`+`SUPABASE_ANON_KEY` (Secure) eklendi (React textarea `fill` no-op → native-setter+dispatch ile). **GERÇEK BUG bulundu+düzeltildi:** build #1 imzalamayı geçti ama `Build signed IPA`'da pod install patladı → **`workmanager_apple` iOS ≥14.0 istiyor, proje 13.0'daydı**; `ios/Runner.xcodeproj/project.pbxproj` `IPHONEOS_DEPLOYMENT_TARGET` 13.0→14.0 (3 config), commit **`b210eb3`** main'e push. **Build #2 (b210eb3) BAŞARILI** (11m20s): analyze+test+imzalama+pod+xcodebuild+IPA (30.49MB)+Publishing hepsi ✓ → ASC'de **Version 1.0.0, Build 2, Processing**. `codemagic.yaml` `submit_to_app_store:false` olduğu için incelemeye OTOMATİK GÖNDERİLMEDİ. **KALAN (kullanıcı aksiyonu):** Apple processing bitince build'i sürüme ekle → TestFlight gerçek-cihaz smoke test → Diyanet meal/TDV Ansiklopedi/EveryAyah sesleri için lisans/izin kanıtı hazırla → **Content Rights = "Yes, third-party content" seç, hak onayını ancak lisans varsa işaretle (Guideline 5.2 IP riski; Arapça metin serbest ama meal/ses/ansiklopedi telifli)** → Submit for Review manuel bas. STORE_SUBMISSION.md §1 güncellendi.
> **Active Context (2026-07-16 — Feed revizyonu + Ayet Bulucu buton düzeltmeleri, KOD+DEPLOY TAMAM / cihaz E2E bekliyor):** Kullanıcı video tanımayı cihazda test etti: ÇALIŞIYOR (ayet %58 eşleşme, Kaydet→koleksiyon OK). 3 bozuk aksiyon + feed revizyonu istendi. **YAPILAN — Bölüm A (butonlar):** (1) Ayet Bulucu ekranından **URL alanı KALDIRILDI** (`_UrlField`+`_urlCtrl` silindi; Instagram giriş duvarı + App Store 5.2.3 yüzünden ölü yoldu; `fromUrl` paylaş-intent için repo'da KALDI). (2) "Okuyucuda Aç" → `context.push('/quran')` (liste) yerine `Navigator.push(SurahReaderScreen(surah, initialAyah))` — `SurahReaderScreen`'e `int? initialAyah` + `GlobalKey`+`Scrollable.ensureVisible` post-frame kaydırma + solup giden altın vurgu (`AnimatedContainer`). (3) "Videoya Aktar" → `StudioScreen(initialText, initialReference)` (yeni opsiyonel param'lar; initState `_overlayText`+`_textController` önyükler). Router'a DOKUNULMADI (const builder → Navigator.push+constructor kalıbı). **Bölüm B (glassmorphism toggle):** yeni `GlassSegmentedToggle` widget (`widgets.dart`, `dart:ui` ImageFilter.blur 18 + kayan altın gösterge `AnimatedAlign` + reduced-motion) FeedScreen'deki `GoldChip` pill'leri değiştirdi; içerik geçişi `AnimatedSwitcher`+`KeyedSubtree(ValueKey(_tab))`. **Bölüm C (Reels Instagram):** (1) çift dokun→beğen + ortada `flutter_animate` kalp; (2) `reelsMutedProvider` (Notifier, varsayılan SESSİZ — Instagram gibi) + `_ReelPage` `StatefulWidget`→`ConsumerStatefulWidget`, `setVolume` init+canlı; sağ üst `_MuteButton`; (3) `VideoProgressIndicator` (Flutter built-in) alt ilerleme çubuğu; (4) **yorumlar**: `comments` tablosu (`sync_comment_count` trigger = `sync_like_count` ikizi, RLS likes deseni, realtime) + `feed_posts.comment_count`; `ISocialRepository.fetchComments/addComment` (tablo yoksa try/catch graceful); `CommentsSheet` (DraggableScrollableSheet, liste+giriş) hem Reels overlay hem `_FeedItemCard`'a bağlı (`_FeedItem.onComment/commentCount`). **DOĞRULANDI:** `flutter analyze` 0; **31 test yeşil** (yeni `glass_toggle_test`); `comments` migration **DEPLOY EDİLDİ** (`supabase db push`, REF uytqxgcbohdlwwxemgyy); APK **derlendi** (`build/app/outputs/flutter-apk/app-debug.apk`) ama **cihaz bağlantısı düştü → kurulum + E2E BEKLİYOR** (Samsung SM S731B tekrar bağlanınca `adb install -r`). Plan: `~/.claude/plans/kur-an-daki-mesaj-video-staged-bumblebee.md`.
> **Active Context (2026-07-15 — Sesli Ayet Bulucu: VİDEODAN tilavet tanıma, KOD TAMAM/DEPLOY+E2E BEKLİYOR):** Kullanıcı "video linkinden okunan ayetleri + meali bulan sistem" istedi (ham plan: whisper-quran-v1 + FFmpeg + linkten sunucu indirme). **Fable 5 ile yeniden planlandı, Opus ile kodlandı.** Araştırma (2 alt-ajan, kanıtlı) ham planın 3 ayağını çürüttü: (1) "whisper-quran-v1" diye HOSTED model YOK (`tarteel-ai/whisper-base-ar-quran` var ama HF'de "not deployed by any Inference Provider" → çağrılamaz); (2) **linkten video indirmek App Store 5.2.3 + Play ihlali** ("download media from YouTube..." birebir yasak) + yt-dlp datacenter IP'de bot-duvarına takılıyor (Supabase EF tam datacenter IP); (3) FFmpeg EF'de imkânsız (native binary yok, wasm 20MB bundle+2sn CPU'ya sığmaz) VE gereksiz — **Groq Whisper mp4'ü DOĞRUDAN yutar + `url` parametresiyle video baytları EF belleğinden hiç geçmez** (256MB/2sn tavanı baypas). **Karar (kullanıcı onayı):** giriş = cihazdaki video (galeri + paylaş menüsü — WhatsApp/Telegram/kaydedilmiş klip), kapsam ≤3dk/≤20MB. Bu bir transcription değil **retrieval** problemi: mevcut IDF-F1 `matcher` (artık `_shared/quran/`) ASR gürültüsünü + uthmani↔modern imla farkını yutar. **YAPILAN:** `matcher.ts`+`quran_data.ts`→`_shared/quran/` taşındı (iki EF ortak kullanır); matcher'a uthmani→modern normalizasyon eklendi (`صلوة→صلاه` vb.) + `ayahByRef`/`surahName` export; **yeni** `ayah-finder-audio` EF (JWT→path sahiplik doğrulaması 403, service_role signed URL 300s→Groq `whisper-large-v3-turbo` verbose_json lang=ar, `url` param + multipart fallback, iş sonu storage.remove); **yeni** saf `segments.ts` (ASR segment→pencereleme→baskın sure oylaması→monoton çapa→aralık+gap-fill+kenar genişletme→timeline; besmele/istiaze filtresi; matched/ambiguous/notFound eşikleri); **yeni** private `ayah-audio` bucket migration (public=false, 20MB, RLS yalnız kendi klasörü); Flutter: `AyahRange`+`AyahTimelineEntry`+`AyahFinderResult` genişletme (geriye/ileriye JSON uyumlu, `AyahMatch` değişmez), `findFromVideo` (20MB ön kontrol→upload→invoke), controller `fromVideoGallery`/`fromVideo`, `SharedAyahInput.videoPath`, `app.dart` `SharedMediaType.video` dalı, AndroidManifest `video/*` intent-filter, ekran "Video Seç" kartı + aralık başlığı + zaman çizelgesi + 7 yeni hata kodu + Paylaş butonu. **DOĞRULANDI:** `flutter analyze` 0 sorun; **Flutter 37 test yeşil** (yeni: `ayah_finder_result_json_test` 5, `ayah_finder_controller_test` 3); **Deno 18 test yeşil** (`deno` 2.9.2 kuruldu; matcher 8 + segments 10); `deno check` EXIT 0. **DEPLOY TAMAM (2026-07-16):** `supabase db push` → `ayah-audio` bucket+RLS canlı (migration `20260714120000`); `ayah-finder-audio` Edge Function **ACTIVE** (v1, verify_jwt); `GROQ_API_KEY` secret **SET** (kullanıcı Dashboard'dan ekledi). Sunucu smoke testi (anon key + REST) DOĞRULANDI: boş gövde→`no_video` 400 (=GROQ key okunuyor, aksi halde `no_api_key` dönerdi), path+anon(kullanıcısız)→`auth_required` 401 (=sahiplik kapısı aktif). CLI: `npx supabase@latest` (yerel kurulu değil), proje linkli, stored token ile auth. **KALAN (cihaz E2E):** Faz 0 gerçek Groq mp4/url smoke'u ancak gerçek kullanıcı JWT + tilavet videosuyla test edilebilir (kod multipart fallback'li → url-param belirsizliği bloklamaz); Samsung SM S731B'de galeri/paylaş/20MB-üstü/sessiz-müzikli/uçak modu, <20sn hedef. **GÜVENLİK BORCU:** `GEMINI_API_KEY` hâlâ rotate edilmeli. Plan: `~/.claude/plans/kur-an-daki-mesaj-video-staged-bumblebee.md`.
> **Active Context (2026-07-01 — Palet tek-kaynak + "Anla·Düşün·Paylaş" şeridi):** `ui-ux-designer` incelemesi uygulandı. (1) **Marka kiti palet hizalama:** `brand/kurandakimesaj-brandkit.html` tek doğru kaynak `app_colors.dart`'a çekildi (zümrüt #1B7A56→#1E4D38, altın #C9A24B→#D4B25B, krem #F4EBD7→#F3EADB, ink #0A130F→#08201A, logo gradyanı #1F8A61→#0D4632 yerine #1E4D38→#08201A, rgba altın 201,162,75→212,178,91) → App Store ikonu↔açılış renk kopukluğu giderildi; `DESIGN.md` zaten doğruydu. (2) **`AyetActionBar` (Anla·Düşün·Paylaş) çekirdek-döngü şeridi** + `showReflectionSheet` ("Düşün" not sheet'i, mevcut `Collections.note` alanına yazar — yeni migration yok) `widgets.dart`'a eklendi; Home Günün Ayeti kartı + daily_ayah ekranına bağlandı (eksik "Düşün" ayağı dolduruldu). **Kod inceleme (code-reviewer + error-detective + /investigate):** `_ReflectionSheet._save` try/catch'siz idi → repo insert throw'unda **sessiz veri kaybı + kalıcı "Kaydediliyor…" asılı buton** (gerçek defect, iki ajan doğruladı) → try/catch + hata SnackBar + `_saving` geri alma; ayrıca `_AyetAction` InkWell'i şeffaf `Material`'a sarıldı (AppCard/GoldChip ripple deseni). `flutter analyze` 0, **21/21 test** (yeni `test/reflection_sheet_test.dart` fix'i kilitler). /qa bağlı cihaz gerektirdiğinden atlandı (statik + tüm test paketi + 2 bağımsız ajan ile doğrulandı).
> **Active Context (2026-07-01 — Profil + Ayarlar + Hesap Silme Sprint, KISMİ):** "Profil çok boş, ayarlar + hesap silme butonu" isteğiyle planlandı (plan: `~/.claude/plans/ecc-architect-...-eager-flurry.md`). **TAMAMLANAN (cihazda doğrulandı, Samsung SM S731B):** (1) **Ayarlar ekranı** `lib/features/settings/settings_screen.dart` (route `/settings`, router.dart'a eklendi) — Tercihler (meal `GoldChip`/bildirim `Switch`/ilgi alanı chip'leri, mevcut `SettingsController` mutator'larına bağlı), Hakkında & Yasal (sürüm kartı + gizlilik/koşullar/KVKK/iletişim satırları; yasal belgeler henüz dialog placeholder, iletişim mailto), Hesap (Çıkış + **Hesabı sil** kırmızı). (2) **Profil girişi** `UserProfileScreen` (community_screens.dart) — kendi profilinde AppBar Ayarlar dişlisi (→/settings) + isim yanında düzenle kalemi (`_showEditNameDialog` → `updateDisplayName` → `_load()`). (3) **Hesap silme** — `SupabaseService.deleteAccount()` (services.dart) + `AuthRepository.deleteAccount()` (repositories.dart) → `delete-account` Edge Function (`supabase/functions/delete-account/index.ts`, service_role ile `auth.admin.deleteUser`, cascade FK temizliği; config.toml verify_jwt=true). UI onay dialog'lu. `dart analyze` 0, **20/20 test**, fixed APK kuruldu+görsel doğrulandı. **DEPLOY BEKLİYOR:** `supabase functions deploy delete-account` (deploy edilmeden hesap silme çalışmaz, ama UI+onay akışı hazır). **TEMA KOYU/AÇIK TAMAMLANDI (2026-07-01, cihazda doğrulandı):** Gerçek açık tema — anlamsal renk rolleri. `app_colors.dart` brightness-duyarlı facade'e çevrildi: tek global palet (`AppColors.brightness` setter) koyu/açık arasında döner, çağrı yerleri (`AppColors.emerald950` vb.) DEĞİŞMEDEN kalır (getter aktif palete bakar). Yüzey+metin+gradyanlar döner; gold ailesi SABİT marka; yeni **`onGold`** (gold buton/chip üstü koyu metin, DÖNMEZ — ~13 `emerald950` kullanımı buna taşındı: community/khatm/progress/quran/widgets/home FAB) + **`goldInk`** (gold METİN, açık temada AA için koyulaşır — eyebrow/arabicStyle). `buildAppTheme(Brightness)` parametreli, `theme`+`darkTheme`+`themeMode` `app.dart`'ta. **KRİTİK:** brightness `MaterialApp.builder`'da themeMode+platformdan hesaplanıp global palete yazılır (`Theme.of(builder-context)` GÜVENİLMEZ). `const` widget'lar global paleti canlı yakalamadığından builder child'ı `KeyedSubtree(key:ValueKey(isDark))` ile sarılır → tema değişince tüm ağaç yeniden build olur (go_router delegate route'u korur). `AppSettings.themeMode`(ThemeMode) + PrefsService(`theme_mode`) + `SettingsController.setThemeMode`; Ayarlar → Görünüm/Tema (Sistem/Açık/Koyu `GoldChip`). Sadece 2 gerçek de-const (onboarding hero gradient, prayer cream2 Icon) + `const AppBarTheme`→non-const. `dart analyze` 0, **20/20 test**. **HENÜZ YAPILMADI (plandan, backend deploy gerektirir):** avatar yükleme (Storage `avatars` bucket+RLS), bio (`profiles.bio` migration), mesajlar yeni-sohbet akışı (`createConversation`+`create_direct_conversation` RPC+kullanıcı arama), ölü `ProfileScreen` (home_screens.dart:757-886) silme (isim-düzenleme mantığı UserProfileScreen'e taşındı, güvenle silinebilir).
> **Active Context (2026-07-01 — GOOGLE GİRİŞİ ÇÖZÜLDÜ, kök neden DEVELOPER_ERROR DEĞİLDİ):** `/investigate` ile cihaz logcat'i (Samsung SM S731B) yakalandı → Google girişi ASLINDA BAŞARILI oluyordu (`signInWithIdToken` tamamlanıyor, Supabase oturumu açılıyor; Profil ekranı `yiit55400` olarak giriş yapılmış gösterdi = oturum kalıcı). Gerçek hata: başarıdan sonraki `context.pop()` (`home_screens.dart` `_AuthScreenState`), `/auth`'a `go()` ile gelindiğinde `GoError: There is nothing to pop` fırlatıyordu (community_screens.dart:1401 `context.go('/auth')` yığını değiştiriyor → pop edilecek route yok); bu navigasyon hatası geniş `catch (e)` tarafından yakalanıp **"Google ile giriş başarısız. Yapılandırmayı kontrol edin."** diye YANLIŞ etiketleniyordu. Önceki oturumun "DEVELOPER_ERROR / Play Services yayılma gecikmesi" teşhisi YANLIŞTI — o logcat dış-config düzeltilmeden ÖNCEki denemedendi; config düzeldikten sonra giriş çalışıyordu ama pop bug'ı başarıyı başarısız gösteriyordu. **ÇÖZÜM:** `_afterAuthSuccess()` yardımcısı (`canPop()` ise pop, değilse `context.go('/home')`); `_submit` (e-posta, satır 984) ve `_googleSignIn` (satır 1001) blind `context.pop()`'ları bununla değiştirildi → push/go her iki giriş yolunu da kapsar. Navigasyon artık try içinde patlamadığı için yanlış-etiketleme imkânsız. Geçici `GAUTH_TEST` debug log'ları temizlendi. Regresyon: `test/auth_navigation_test.dart` (canPop=false yığında başarılı giriş → hata yok + /home'a gider; düzeltme olmadan BAŞARISIZ olduğu doğrulandı). `dart analyze` 0, **20/20 test**, fixed APK cihaza kuruldu. Google config (SHA-1 `5B:7E...:FA:A4`, Web client, Supabase provider) zaten DOĞRUYDU — değişiklik gerekmedi. **Kullanıcının "google bağlantı sorunu" şikâyeti = bu UI yanlış-etiketleme bug'ıydı; giriş baştan beri çalışıyordu.**
> **Active Context (2026-06-30 deploy TAMAM):** Ayet Bulucu backend tamamen canlı ve uçtan uca DOĞRULANDI. `ayah-finder` Edge Function **DEPLOY EDİLDİ** (config.toml'a `[functions.ayah-finder] verify_jwt=true`). `follows` migration **UYGULANDI** (`supabase/migrations/20260630120000_follows.sql` → `db push`; tablo+RLS canlıda). `GEMINI_API_KEY` secret **SET EDİLDİ**. **KRİTİK MODEL DEĞİŞİKLİĞİ:** `gemini-2.0-flash` ücretsiz tier'dan kaldırılmış (`free_tier_requests limit:0` → 429); index.ts modeli **`gemini-2.5-flash`** yapıldı (free tier'da aktif, doğrulandı). Canlı uçtan uca test: dummy görsel → `{status:notFound, extractedArabic:""}` (Gemini çağrısı hatasız geçti). **GÜVENLİK:** Gemini key'leri sohbette düz metin paylaşıldı (`AQ.Ab8RN6...` formatı, AI Studio key'i) → **rotate önerildi**. **GOOGLE AUTH (2026-06-30 — kullanıcı bildirdi: dış config TAMAM):** Kod+env.json zaten hazırdı (`signInWithGoogle()` services.dart:161 google_sign_in 7.2 native akış → `signInWithIdToken`; `env.json` `GOOGLE_WEB_CLIENT_ID=1017467126696-vh3c6h4h53d4lmkqpnenqpmvbtlt192e.apps.googleusercontent.com`; applicationId `com.kurandakimesaj.app`). Kullanıcı iki dış işi YAPTI: (1) Google Cloud Console → OAuth Android client (package `com.kurandakimesaj.app` + debug SHA-1 `5B:7E:12:3A:B1:97:75:82:2A:1D:34:04:FB:7A:2A:2E:17:5B:FA:A4`), (2) Supabase Dashboard → Auth → Providers → Google etkin + Web client ID/secret. **CİHAZ E2E TESTİ YAPILDI (2026-06-30, Samsung SM S731B) → DEVELOPER_ERROR, HENÜZ ÇÖZÜLMEDİ.** Logcat: `GoogleApiManager ... ConnectionResult{statusCode=DEVELOPER_ERROR}` (=ApiException:10). **TÜM CONFIG GÖRSEL OLARAK DOĞRULANDI, hepsi doğru:** debug keystore SHA-1 (`keytool` ile `~/.android/debug.keystore`) = `5B:7E...` Cloud Console ile birebir; package `com.kurandakimesaj.app` eşleşiyor; Cloud `kdm-web` client tipi = **Web application** (`1017467126696-vh3c6h4h53d4lmkqpnenqpmvbtlt192e`, env.json ile aynı); `kdm-android` (`1017467126696-irfhvf18o0a64qdtjgbp20fb8p0t0i47`); OAuth consent screen **In production** (External, test-user sorunu yok); Supabase Google provider Web client ID+secret dolu (native akışta idToken audience=Web client → yeterli). SHA-1/package/Web-client-tip/consent-screen hipotezlerinin HEPSİ elendi. **Kalan tek açıklama: Google OAuth değişikliklerinin Play Services'e yayılma gecikmesi** (Android client Jun 29 oluşturuldu; Google notu 5dk–birkaç saat). **Sıradaki:** cihazda Play Hizmetleri önbellek temizle + reboot + retry; olmazsa birkaç saat/ertesi gün tekrar (config doğru olduğundan zamanla çalışmalı); ayrıca Supabase "Client IDs" alanına Android client ID virgülle eklenmesi önerildi (ileride). Test sırasında DEVELOPER_ERROR'ı tekrar tetiklemek faydasız. `GOOGLE_WEB_CLIENT_ID` env.json'da mevcut. Proje ASCII yolda (`C:\Users\yiit5\Desktop\kurandaki mesaj flutter\kurandakimesaj`). Supabase canlı (REF `uytqxgcbohdlwwxemgyy`). **Kalan manuel:** ~~Google OAuth Android client + SHA-1 + Supabase Google provider~~ (kullanıcı YAPTI 2026-06-30, cihaz E2E doğrulaması bekliyor); iOS widget/Share Extension target (macOS/Xcode). Paketler: image_picker 1.2.2, **speech_to_text 7.4.0** (6.6.2'den yükseltildi — eski Flutter v1 embedding API'si derlenmiyordu), file_picker 12.0.0-beta, path_provider, flutter_tts 4.x, **record 7.x, receive_sharing_intent 1.8.1**, **home_widget 0.7.0+1, workmanager 0.9.0+3** (ana ekran widget'ları). **Android build araç zinciri:** AGP **8.11.1**, Kotlin **2.2.20**, Gradle wrapper 8.14.1, build-tools 35.0.0, `JAVA_HOME`=Android Studio jbr (`C:\Program Files\Android\Android Studio\jbr`). Supabase secrets'ta `GROQ_API_KEY`+`GEMINI_API_KEY` (ayet tanıma Edge Function'ları için). **Sıradaki:** ayet tanıma Edge Function'larını deploy + cihaz E2E + iOS Share Extension (ertelendi); follows_migration.sql Supabase'e deploy, RevenueCat/render worker, Android derin bağlantı (OAuth), onboarding "aha", post-media bucket public RLS politikası, **iOS widget extension target + App Group + iOS workmanager arka plan (Xcode/macOS manuel — `ios/KuranWidget/SETUP.md`)**.

---

## [1. PROJECT VISION & GOALS]
* **Core Concept:** Kur'an okuma + günlük ibadet + öğrenme + "ayet → paylaşılabilir kısa video stüdyosu" + topluluk/akış birleştiren manevi mobil uygulama. Döngü: **Anla → Düşün → Paylaş**.
* **Target Audience:** Türkçe konuşan kullanıcılar; 3 persona — Genç Paylaşımcı (16-28), Düzenli Kullanıcı (25-45), Öğrenen (her yaş).
* **Success Criteria:** **26 özellik** çalışır (tek kaynak `lib/app/feature_catalog.dart`; sayı ihtilafında kod kazanır); farklılaştırıcı = ayet kartı stüdyosu + AI Ayet Bul + Reels akışı.
  _NOT (K1):_ "ayet→video **render**" kapsam dışıdır (ffmpeg_kit emekli). Stüdyo çıktısı `still` reel'dir; kullanıcı video seçerse video ARKA PLAN olur ve ayet metnini oynatıcı üste çizer. Kullanıcıya "video üretiyoruz" vaadi verilmez.
* **Platform:** iOS + Android (tek kod tabanı).

## [2. TECH STACK & CONSTRAINTS]
* **Language/Framework:** Flutter 3.44 · Dart 3.12.2
* **Backend/DB:** **Supabase** (Postgres + Auth + Storage + Realtime + Edge Functions). Ayrı özel backend YOK — BaaS yeterli. AI proxy/sunucu mantığı Edge Functions.
* **State Management + DI:** **flutter_riverpod 3.x** — `Notifier`/`AsyncNotifier` = ViewModel, provider grafiği = DI konteyneri (get_it yok).
* **Routing:** **go_router 17** — `StatefulShellRoute.indexedStack` (5 sekme + ortada FAB).
* **Yerel kalıcılık:** **drift** (SQL + FTS5) + **shared_preferences**. _(NOT: Isar yerine drift — bkz. Karar Günlüğü.)_
* **Modeller:** freezed + json_serializable (codegen). Sprint 0'da AppSettings plain immutable.
* **Abonelik:** purchases_flutter (RevenueCat) — Sprint 3'te devreye girer.
* **Animasyon:** flutter_animate + Hero + implicit + CustomPainter (tesbih/dairesel/kıble) + rive + lottie.
* **Constraints:** Offline-first dini içerik; Türkçe tam ortografi (ç/ğ/ı/ö/ş/ü); Arapça RTL + Amiri Quran; ≥44px dokunma; secret'lar koda gömülmez.

## [3. ARCHITECTURE & PATTERNS]
* **Design Pattern:** Katmanlı (UI / Domain / Data) + Riverpod-as-ViewModel. Akış: `View(ConsumerWidget) → ref.watch(vmProvider) → Notifier → Repository → Service(Supabase|drift|Device)`. View asla Service'e doğrudan dokunmaz.
* **Folder Structure (hibrit — data/domain tipe göre, ui feature'a göre):**
    * `lib/app/` — app.dart, router.dart, feature_catalog.dart (bootstrap app.dart içinde)
    * `lib/data/` — services.dart (Supabase/Prefs/Permission), local/app_database.dart (drift), seed/seed_service.dart, repositories.dart (+ Riverpod provider'ları)
    * `lib/domain/` — models.dart (AppSettings, enum'lar); use_cases/ (sonra)
    * `lib/ui/core/` — theme/ (app_colors, app_theme), widgets.dart, painters.dart
    * `lib/features/<feature>/` — onboarding, home (shell+sekmeler+auth+katalog); diğerleri Sprint 1+
* **Naming:** Dart standardı (lowerCamelCase / UpperCamelCase / snake_case.dart).
* **Tasarım sistemi:** Zümrüt (#08201A/#0D2A20/#1E4D38) + Altın (#D4B25B/#E8CB6F) + Krem (#F3EADB). Fontlar: Cormorant Garamond / DM Sans / Amiri Quran.

## [4. ACTIVE RULES (The "Laws")]
1. API key / secret asla koda gömülmez — `.env`/`--dart-define`/Supabase Function Secrets. `service_role` asla istemcide.
2. Kod yazmadan önce bu dosyayı oku; mimari karar/görev bitişinde güncelle.
3. Arapça metin RTL + Amiri Quran fontuyla render edilmeli.
4. Tüm kullanıcı metinleri Türkçe + tam ortografi (ç/ğ/ı/ö/ş/ü).
5. **RLS her Supabase tablosunda açık (default-deny).** Dini metin yazılabilir tabloda durmaz (drift seed, değiştirilemez).
6. **Proje ASCII yolda tutulmalı** (`...\Desktop\kurandaki mesaj flutter\...`) — `ü` içeren yol gen_snapshot/AOT'u kırar. ÇÖZÜLDÜ: proje Desktop'a (ASCII) taşındı; junction artık gerekli değil. Flutter PATH: `C:\Users\yiit5\flutter\bin`.
7. Mimari katman akışını koru: View→Notifier→Repository→Service.

## [5. PROGRESS & ROADMAP]
- [x] **Sprint 0 — İskelet:** tema, core widget'lar+painters, go_router 5-sekme shell+FAB, onboarding (splash+3 adım kurulum), Supabase init+auth, drift open+seed, 24 özellik route placeholder, Tüm Özellikler kataloğu. `flutter analyze` 0 sorun, 3 test geçti.
- [x] **Sprint 1 — Günlük çekirdek:** Ana Sayfa (gerçek namaz şeridi+Günün Ayeti+istatistik), Namaz Vakitleri (adhan+geolocator+geri sayım+ezan bildirimi), Zikirmatik (TasbihPainter canlı+haptik+drift sayaç), Tesbihat (33-33-33), Günün Ayeti&Hadis (share+kaydet), Kıble (compass+fallback), Dini Günler (hijri+kandil geri sayımı). Bildirim servisi (flutter_local_notifications 22).
- [x] **Sprint 2 — Kur'an & öğrenme:** Kur'an Okuma (114 sure meta + küratörlü ayet metni + tilavet just_audio + sepya), Cüz/Hizb takip, Esma (99), Dua, Kıssalar, Sure Ezberi, Mucizeler, Konuya Göre, Oruç&İmsakiye, Koleksiyonlar (drift). Seed tam içerikle genişletildi (`seed_data.dart`).
- [x] **Sprint 3 — Stüdyo & akış:** Şablon Galerisi, Video Edit+Meal stüdyosu (9:16 canlı önizleme, render kuyruğu — sunucu taraflı), Akış (seed içerikten dolu, like/share), AI Asistan (Edge çağrısı + yerel öneri fallback). PRO kapısı (`proProvider` + `ProUpsell`) — RevenueCat soyutlaması.
- [x] **Sprint 4 — Topluluk & araçlar:** Mesajlar (sohbet + Realtime notu), Hatim Halkaları (30 cüz dağıtımı), Zekât (canlı %2,5 + nisab + uyarı), Cami Bul (konum→harita), Bağış (kampanya+url_launcher+sadaka bildirimi), Rüya Tabiri (FTS arama + uyarı), Tecvid (PRO+quiz).
- [x] **Cihaz QA (2026-06-15):** Uygulama Samsung SM S731B'de `flutter run` ile **derlendi ve açıldı**. Android: minSdk 23, core library desugaring (flutter_local_notifications 22), izinler (konum/bildirim/internet) eklendi.
- [x] **Backend DEPLOY EDİLDİ (2026-06-16) — REF `uytqxgcbohdlwwxemgyy`:** `npx supabase` 2.106.0 ile `link` + `db push` (2 migration → 15 tablo + RLS + Storage + Realtime) + `functions deploy` (5 Edge Function: render-trigger/status, donation-verify, daily-content, revenuecat-webhook) canlıya alındı. Docker gerekmedi (API deploy). Login kullanıcı kendi terminalinde yaptı. Eksik (opsiyonel): RevenueCat secret, pg_cron daily-content (dashboard SQL, service_role kullanıcıda).
- [x] **Cihaz görsel QA (2026-06-16) — otonom:** Uygulama SM S731B'de Supabase'li açıldı (`--dart-define=SUPABASE_URL/ANON_KEY`). `flutter screenshot` + adb input ile gezilip incelendi (`qa_screens/`). **3 bug cihazda doğrulandı:** Profil çökmüyor (Misafir+PRO+Giriş kartları), Ana ekran "Hızlı İşlemler" overflow yok, "Tüm Özellikler" kartı → 24 özellik kategorili katalog açılıyor. Spot-check temiz: Namaz Vakitleri, Akış (seed), Mesajlar (seed), Günün Ayeti & Hadis. NOT: adb tap koordinatları screenshot'tan kör tahmin → küçük kart alanına denk getirmek 1-2 deneme gerektirebiliyor (araç sınırı, app bug'ı değil); cihaz şarjda uyandı→`svc power stayon true` ile ekran açık tutuldu (QA sonrası reset).
- [x] **Tasarım incelemesi + Mimari sertleştirme (2026-06-16):** /plan-design-review (tüm plan, 7-boyut, koda çapraz-kontrol) → 3 onaylı karar uygulandı (akış dürüstleştirme, "Videolarım"+paylaş dalı, A11y şimdi). /senior-architect → P1/P2/P3 riskleri **tamamiyle** düzeltildi: drift `onUpgrade` kullanıcı tablolarını koruyor (launch-blocker veri kaybı giderildi), `Supabase.instance` tek gateway'e toplandı, repo arayüzleri + test seam, secret'lar `AppConfig`'e. `dart analyze` 0 sorun, 13 test geçti. Doğrulama: analyze+test yeşil (bug/failing-test olmadığı için `/investigate` uygulanmadı — o kök-neden hata ayıklayıcı). **P1-2 (render-status Edge Function deploy) bilinçli ertelendi** (office-hours: wedge doğrulanana kadar).
- [x] **Backend (kod, kalan ekran bağlama):** Tüm ekranlar Supabase'e bağlandı: MessagesScreen (Realtime watchMessages), KhatmScreen (publicCircles+watchClaims+claim), ProfileScreen (display_name+updateDisplayName), CollectionsRepository (pushCollection sync). `dart analyze` 0 sorun.
- [x] **Büyük Sprint (2026-06-20) — UI/UX Pro Max:** A1 esma hata fix, A2 chip overflow fix (52→56px). C1 PRO gate tamamen kaldırıldı (ProController/ProUpsell/isPro — 7 dosya). C2 şablon galerisi silindi. B1 mucizeler 10→30, B2 kıssalar 8→20, B3 rüya 20→54. G1 zekat GoldPriceService+Hanafi/Şafi toggle. Paketler: image_picker/speech_to_text/file_picker/path_provider. D1 StudioScreen Canva editörü (image_picker+RepaintBoundary+Supabase Storage+9:16/1:1). E1 speech_to_text+duygu haritası+AiAssistantScreen+HomeScreen altın FAB+platform izinleri. F1 CreatePostSheet görsel/video yükleme+FeedItemCard thumbnail. H1 PrayerTracker namaz takip, H2 ReadingGoalService okuma hedefi+streak, H3 story/kare toggle.
- [x] **H4-H10 Sprint (2026-06-20):** Drift schemaVersion 2→3 (ReadingEvents+FavoriteSurahs tabloları, build_runner yeniden üretildi). H4 TtsService (flutter_tts tr-TR, QuranScreen+DailyAyahScreen butonları). H5 SurahQuizScreen (10 soru/4 tip/skor kaydı, /quiz rotası). H6 NotificationService+DuaScreen sabah/akşam hatırlatıcı (scheduleDailyDua). H7 kIslamicDays2026+HolyDaysScreen bildirim toggle (scheduleOnce, ID 100-200). H8 StatsService+ProgressScreen (haftalık grafik CustomPaint, QuranScreen Stopwatch kayıt ≥10s). H10 FavoritesService+QuranScreen yıldız UI+Tümü/Favoriler filtresi. H9 UserProfileScreen+/profile/:id (takipçi/takip/gönderi grid, follows graceful degradation). `docs/supabase/follows_migration.sql` hazır — **manuel deploy gerekli**.
- [x] **Ayet/Sure Tanıma Sprint (2026-06-23):** AI'a iki "ayet tanıma" girdisi — (1) **sesli tilavet** (Groq Whisper Large v3 Turbo, Edge `quran-recognize`), (2) **Instagram görseli/galeri** (Gemini 2.0 Flash OCR, Edge `quran-vision`). Akış: girdi → Arapça metin → `ArabicNormalizer` → **in-memory `QuranMatcher`** (token-coverage + bitişik dizi bonusu, eşik <0.4 düşük güven) → yetkili sure:ayet + Diyanet meali (uydurma yok). **Tam Kur'an asset'i** `tool/fetch_quran.dart` → `assets/quran/quran_full.json` (6236 ayet/2.35MB, AlQuran Cloud uthmani+tr.diyanet). Drift schemaVersion 3→4 (`QuranFullAyahs`). Yeni dosyalar: `lib/domain/arabic_normalizer.dart`, `lib/data/quran_matcher.dart`, `lib/data/services/quran_recognition_service.dart`, `lib/features/ai/ayah_recognition_screen.dart` (mic+görsel, RTL/Amiri kartlar, TtsService dinlet), `supabase/functions/quran-{vision,recognize}/`. `/ayah-recognition` route + AI asistandan giriş chip'i. Paketler: record 5.2.1, receive_sharing_intent 1.8.1. Android share-intent (ACTION_SEND image/*) kablolu; **iOS Share Extension ertelendi** (galeri iki platformda çalışır). Supabase secrets'a key'ler eklendi. `dart analyze` 0 hata, 26/26 test. **Manuel:** `supabase functions deploy quran-vision && quran-recognize` + cihaz E2E.

- [x] **Ana Ekran Widget'ları Sprint (2026-06-30):** Telefon ana ekran widget'ları + uygulama içi yeni kartlar, 4 faz. **Faz 1 (uygulama içi):** `home_screens.dart`'a `_HijriDateCard` (hicri+miladi, →/holy-days) + `_QiblaMosqueCard` (Kıble→/qibla, Cami→/mosque). **Faz 2 (Android widget):** `home_widget 0.7.0`, `lib/data/widget_sync_service.dart` (namaz+hicri+ayet verisini App Group/SharedPreferences'a yazar), Android `PrayerWidgetProvider`+`AyahWidgetProvider` (Kotlin, RemoteViews+layout/xml/drawable), manifest receiver'ları, deep-link `homeWidget://prayer|ayah`. Senkron `HomeScreen.build` içinde `ref.listen` ile tetiklenir. **Faz 3 (iOS):** `ios/KuranWidget/KuranWidgets.swift` (WidgetKit bundle+PrayerWidget+AyahWidget+tema), Info.plist/entitlements, `Runner.entitlements` App Group, `homeWidget` URL şeması. **Faz 4 (arka plan):** `workmanager 0.9.0`, `lib/data/widget_background.dart` (`@pragma('vm:entry-point')` dispatcher → ezan widget'ı 15dk yenileme), konum prefs'e kaydedilir (background geolocator gerekmez), `bootstrap()`'ta init. `dart analyze` 0, **18/18 test**, Android debug APK derlendi. **iOS Faz 3+4 = macOS/Xcode manuel adımları (`ios/KuranWidget/SETUP.md`).**
- [x] **AGP 9 build düzeltmesi (2026-06-23):** `flutter run` Android build'i AGP 9.0.1 / `newDsl` çakışmasından çöküyordu. AGP 9.0.1→8.11.1 + Kotlin 2.3.20→2.2.20 (settings.gradle.kts), SDK lisansları kabul edildi (build-tools 35 engeli), `speech_to_text` 6.6.2→7.4.0 (eski v1 embedding derlenmiyordu). Doğrulandı: `flutter build apk --debug` → `app-debug.apk` (192MB) üretildi. Detay Karar Günlüğü'nde.

- [x] **UI/UX İnce Ayar (2026-07-01) — Palet tek-kaynak + çekirdek döngü şeridi:** `ui-ux-designer` ajanının değerlendirmesinden iki uygulama. (A) **Palet hizalama:** marka kiti (`brand/kurandakimesaj-brandkit.html`) ile kod paleti (`app_colors.dart`) ayrışmıştı (canlı #1B7A56 vs küllü #1E4D38 zümrüt, #C9A24B vs #D4B25B altın vb.) → kod tek doğru kaynak kabul edilip HTML ona hizalandı; logo SVG gradyanı da `heroGradient` ailesine (#1E4D38→#08201A) çekildi, ayrışmış swatch metin-kontrastı düzeltildi. (B) **`AyetActionBar` (Anla·Düşün·Paylaş)** paylaşılan widget + `showReflectionSheet` (Düşün → `Collections.note`, migration gerekmedi) `widgets.dart`'a eklendi; Home Günün Ayeti + daily_ayah'a bağlandı (marka döngüsü her ayet yüzeyinde görünür, eksik "Düşün" ayağı doldu). Home'da "Devamı"→Anla, daily_ayah'ta eski [Paylaş|Kaydet] Row'u şeride devredildi, Anla→`_showTafsir` (ayah.tafsir varsa sheet). **Doğrulama (code-reviewer + error-detective):** gerçek defect — `_ReflectionSheet._save` hata yolunu ele almıyordu (insert throw → sessiz not kaybı + asılı buton) → try/catch + SnackBar + `_saving` reset; `_AyetAction` `Material(transparent)>InkWell` desenine uyduruldu. Regresyon `test/reflection_sheet_test.dart` (onSave throw → buton kilitlenmez + hata görünür; fix olmadan düşer). `flutter analyze` 0, **21/21 test**.

- [x] **1.0.1 — Apple Guideline 1.2 yanıtı (2026-07-25/26):** Moderasyon kuyruğu (RLS + trigger, veritabanı seviyesinde), yorum moderasyonu, bildir/engelle aksiyonları reel aksiyon rayında **etiketli buton** olarak (menüye gömülü değil — ASC metninde böyle beyan edildi), engellenenler listesi + engel kaldırma, `/moderation` yönetici ekranı (Bekleyenler + Şikâyetler sekmeleri), giriş ekranında zorunlu EULA onay kutusu (üç giriş yolunu da kilitler), stüdyoda telif beyanı kutusu (Guideline 5.2.3, **her iki** yayınlama yolunda), Sign in with Apple (Guideline 4.8), `kind` sadeleştirmesi (`video`/`still`), 20MB video sınırı (istemci + bucket), SSRF sertleştirmesi. 3 migration üretimde; `terms.html` yayında; Apple capability + provisioning profile yenilendi; Codemagic saklanan profil tazelendi. `flutter analyze` 0, **flutter test 92/92**, `deno test` 27/27. Ayrıntı: `docs/APP_REVIEW_RESPONSE.md`.

## [6. DECISION LOG & ANTI-PATTERNS]
* **[2026-08-03 — Faz B/C: onay beklemeden uygulandı, GÖNDERİM SIRASI AYRI TUTULMALI]:** Kullanıcı "bütün fazları bitir" dediği için madde 1/4/2 (Faz B) ve 7-tam/6/5 (Faz C) build 4 onayı gelmeden uygulandı. Planın sıralaması yine de geçerli: **Faz A'yı ayrı gönder** — madde 2 yeni bir arka plan yeteneği (`UIBackgroundModes: audio` + foreground service), madde 6 yeni bir görsel düzenleme yüzeyi açıyor; ikisi de redde yanıt turunda ikinci bir inceleme konusu yaratır. Üç migration da **yazıldı ama uygulanmadı** (Supabase erişimi yok) → gönderimden önce iki oturumlu SQL doğrulaması zorunlu.
* **[2026-08-03 — Güvenlik: RLS'li tabloya politika İÇİNDEN sorgu atmak kapıyı sessizce açar]:** `messages_insert_member`'a engelleme kontrolü eklerken `user_blocks`a düz alt-sorgu atmak İŞE YARAMAZ: alt-sorgu çağıranın rolüyle çalışır ve `user_blocks_select_own` (`auth.uid() = blocker_id`) devreye girer. "Karşı taraf BENİ engelledi" satırının `blocker_id`si karşı taraftır → satır bana görünmez → `not exists` her zaman true → kapı, engelleme varken bile açık kalır ve **testte de fark edilmez** (tek yönü test edersen geçer). Çözüm `is_conversation_member` kalıbı: `security definer` + `set search_path = public` yardımcı fonksiyon (`blocks_between`). Ayrıca politika **DROP edilip yeniden yazıldı**, yanına ikinci politika eklenmedi — permissive politikalar OR'lanır (1.0.1 dersi #1). **Kural:** bir RLS politikasının içinden başka bir RLS'li tabloyu sorguluyorsan, o tablonun politikasının aradığın satırı GİZLEYİP gizlemediğini sor; cevap evetse security definer şart.
* **[2026-08-03 — Telif kapısı uygulamada değil YABANCI ANAHTARDA]:** Reels arka plan müziği için `feed_audio_tracks` tablosuna **hiç yazma politikası yazılmadı** (RLS açık + politika yok = default-deny) → yalnız service_role doldurur; `feed_posts.audio_track_id` FK ile o tabloya kilitli. Kullanıcı keyfi ses URL'i gösteremez, üstelik moderasyon trigger'ına hiç dokunulmadan. `audio_track_id` trigger'ın "onay sonrası değişiklik" demetine **bilerek eklenmedi**: demetteki 9 kolon serbest metin/URL (değişmeleri moderatörün görmediği içerik demektir), `audio_track_id` ise her olası değeri önceden onaylı. Eklersek kullanıcı müziğini değiştirdiği için onaylı reel'i kuyruğa düşürürüz — kazanç yok, ceza var. Gerekçe SQL dosyasına yazıldı.
* **[2026-08-03 — `pro_image_editor` varsayılanı JPG, depo `.png` yazıyor]:** `ImageGenerationConfigs.outputFormat` varsayılanı `OutputFormat.jpg`; `uploadPostMedia` ise dosyayı `.png` adıyla ve `image/png` content-type'ıyla yazıyor → varsayılan bırakılsaydı hem uzantı hem MIME yalan söylerdi ve sorun aylar sonra bozuk dosyalarla ortaya çıkardı. `kStudioEditorConfigs` sabite çıkarılıp birim testiyle kilitlendi (`test/studio_editor_configs_test.dart`); aynı test sticker/emoji/audio araçlarının KAPALI olduğunu da doğruluyor (yeni UGC yüzeyi = Guideline 1.2 riski). Paket 13.3.0 `receive_sharing_intent 1.8.1` pinini **kırmadı**, `flutter build apk --debug` geçti.
* **[2026-08-03 — Anti-Pattern: tembel sliver çocuğu LAYOUT edilir ama BOYANMAZ → `toImage()` patlar]:** Stüdyoda "Reels'e Yayınla" **hiç çalışmıyordu**. Önizleme (`RepaintBoundary` + `_previewKey`) `ListView`in ilk çocuğuydu, yayın butonu ise çok altındaki bir çocuk; 9:16 önizleme ekranı doldurduğu için butona ulaşmak **zorunlu kaydırma** gerektiriyordu. `RenderSliverMultiBoxAdaptor.paint` görünür aralık dışındaki çocuğu **boyamaz** (`sliver_multi_box_adaptor.dart:738-743`). **Mekanizma ölçülerek doğrulandı ve ilk teşhisten farklı çıktı — üç ayrı mod var:** (a) *yalnız kaydırma* boundary'yi kirletmez; `OffsetLayer` **koparılır** (attached=false) ama bayat hâliyle durur ve `toImage()` **çalışır** → release'de çökme yok, **sessizce ESKİ kare yayınlanır** (assert'ten daha sinsi). (b) *Boyanmaz haldeyken kirlenirse* (klavye açıkken metin yazmak — her tuş `setState`; font slider'ı) `PipelineOwner.flushPaint` kopmuş katmanlı düğümü `_skippedPaintingOnLayer` ile atlar ve `_needsPaint` **KALICI** `true` kalır (kaç kare geçerse geçsin temizlenmez) → `RenderRepaintBoundary.toImage`'ın `assert(!debugNeedsPaint)`'i patlar (**`proxy_box.dart:3553`**, `object.dart` değil). (c) Element cacheExtent'i de aşarsa tamamen atılır → `currentContext == null` → `_renderPng()` **null döner**, `_export` sessizce hiçbir şey yapmaz. **Uygulama aslında ÇÖKMÜYOR:** assert `_publish`/`_export`'un `catch (e)`'sine düşüyor, kullanıcı yalnız "Yayınlama hatası: …" SnackBar'ını görüyor ve gönderi hiç yayınlanamıyor. **`endOfFrame` tek başına bu kusuru ÇÖZMEZDİ** — sliver ekran dışındaki çocuğu kaç kare beklenirse beklensin boyamaz; `endOfFrame` yalnız `setState`→`toImage` aynı mikro görev sorununu kapatır. Asıl çözüm önizlemeyi listeden çıkarmaktır. Bir de planın yanlış çıkan varsayımı: `.animate().fadeIn()` boundary'nin **ATASI** olduğu için yakalanan karenin opaklığını etkilemiyordu (`toImage` yalnız boundary'nin kendi alt ağacını rasterleştirir) — silinmesi doğru ama gerekçesi bu değil. **Kural:** `RepaintBoundary`+`toImage()` ile yakalanan hiçbir şey tembel bir sliver'ın (ListView/GridView/CustomScrollView) çocuğu OLMAMALI; kaydırılan listenin dışına, sabit boyutlu bir tuvale koy. Yan kazanç: tuval ekrandan bağımsız olduğu için `pixelRatio: 3.0` artık her cihazda tam 1080×1920 üretiyor (eskiden tuval ekran genişliğine bağlıydı, aynı 22pt farklı telefonda farklı çıktı veriyordu). Ayrıca `setState` yalnız kare PLANLAR: `toImage()` öncesi `await WidgetsBinding.instance.endOfFrame` şart.
* **[2026-08-03 — Anti-Pattern: `go()` yığını siler; erken `push` + tek seferlik provider birbirini yer]:** Soğuk başlatmada paylaşım (Instagram/TikTok → Ayet Bul) açılmıyordu. `app.dart` `_onShared` t≈0'da `push('/ayah-finder')` yapıyor, `SplashScreen._go` t=2400 ms'de `context.go('/home')` ile **yığını tamamen değiştiriyor** → push edilen rota siliniyor. **Asıl tuzak burada değil:** push edilen `AyahFinderScreen` bir frame sonra `sharedAyahInputProvider`'ı okuyup `clear()` ediyor, bu yüzden splash'e "provider doluysa tekrar push et" eklemek TEK BAŞINA çalışmaz — provider o an her zaman null'dur. Doğru düzeltme iki parçalı: `_onShared` **hâlâ `/splash`teyken push etmez** (`router.state.uri.path` kontrolü), rotayı splash üstlenir; provider tüketilmeden hedefe varır. Yavaş cihazda `getInitialMedia` 2400 ms'den geç dönerse konum artık `/home` olduğu için normal push yolu çalışır — iki sıralama da kapalı. `_onWidgetClick` bu hatadan **bağışık** (o `go` kullanıyor, splash kendini `mounted` guard'ıyla iptal ediyor). Reddedilen: router-level `redirect` (push edemez), `singleTask` (recents + `home_widget` deep-link'ini bozar).
* **[2026-08-03 — Anti-Pattern: `catch(_) { return []; }` bir RLS reddini "içerik yok"a çevirir]:** Profil ızgarası boş yeşil kutular gösteriyordu. İki ayrı kusur: (1) `getUserPosts` `thumbnail_url` seçiyordu ama o kolon **hiç yazılmıyor** (`createPost` yalnız `media_url`/`video_url` yazar) — Reels tarafı bunu `FeedPost.fromMap`'teki `thumbnail_url ?? media_url` fallback'iyle çözmüştü, profil ham map kullandığı için fallback'e hiç uğramıyordu. Düzeltme fallback'i ikinci kez yazmak değil, **aynı `fromMap`'ten geçmek**. (2) `catch(_) { return []; }` RLS reddini ve ağ hatasını "Henüz gönderi yok" diye gösteriyordu; hata artık moderasyon dörtlüsündeki gibi çağırana yayılıyor, ekran "Gönderiler yüklenemedi." + "Tekrar dene" gösteriyor. **Kural:** bir `catch` boş koleksiyon döndürüyorsa, o boşluğun kullanıcıya ne söylediğini sor. RLS'e dokunulmadı — `feed_select_visible` zaten doğru davranıyor (kendi pending'ini görürsün, başkasınınkini görmezsin); ızgaraya "İncelemede" rozeti eklendi.
* **[2026-08-03 — Bekleyen tuzak: ikinci `heroTag`'siz FAB uygulamayı düşürür]:** Faz B madde 1 (`HomeScreen`'e sol-alt AI butonu) uygulanmadan önce okunmalı. Bugün projede tek FAB var: `community_screens.dart:214` `FeedScreen`'in FAB'ı ve **`heroTag`'i YOK** (kod okunarak doğrulandı). Router `StatefulShellRoute.indexedStack` kullandığı için Ana Sayfa ve Akış dalları **aynı anda ağaçta canlı** kalır → ikinci bir tag'siz FAB eklenirse `multiple heroes that share the same tag` assertion'ı patlar. **Kural:** `StatefulShellRoute` altında eklenen HER FAB'a açık `heroTag` verilecek (`home_ayah_finder_fab` gibi). `bottom_bar_height_test.dart` bu maddenin gerçek regresyon kapısıdır — `BottomBar`'ın `heightFactor: 1` hücresine dokunulmamalı.
* **[2026-07-26 — Anti-Pattern: `Center` sınırsız yükseklikte BÜYÜR]:** Alt menü çubuğu erişilebilirlik için `SizedBox(height: 64)` → `ConstrainedBox(minHeight: 64)` yapıldı (etiket iki satıra kayınca kırpılmasın, WCAG 1.4.4). Yükseklik serbest kalınca `_fab()` içindeki `Center` — `heightFactor` verilmediği için — gelen `maxHeight`'a kadar büyüdü; `Scaffold`'un `bottomNavigationBar` yuvasında bu **ekran yüksekliği**. Sonuç: bar tüm ekranı kapladı, ikonlar dikeyde ortalandı, gövdeye sıfır yer kaldı → **ana ekran bomboş göründü ve bu haliyle 1.0.1 build 3 olarak App Store Connect'e yüklendi.** Düzeltme: `Center(heightFactor: 1)`. **Kural:** sabit yükseklikli bir kabı esnek yüksekliğe çevirirken içindeki her `Center`/`Align`/`Spacer`'ı gözden geçir. `flutter analyze` ve 91 widget testinin hiçbiri yakalamadı (yalnız cihazda görülüyordu) → `test/bottom_bar_height_test.dart` yazıldı; düzeltme geri alındığında bar 800px (= ekran) ölçülüp düşer.
* **[2026-07-25 — Güvenlik: moderasyon kapısı kolon ayrıcalığıyla korunamaz]:** `revoke update(status) ... from authenticated` işe yaramaz, çünkü **yönetici de `authenticated` rolündedir** — rol bazlı ayrıcalık satır bazlı kuralı ifade edemez. Ayrıca iki izin veren RLS politikası **VEYA**'lanır: `feed_update_own` var olduğu sürece `feed_update_admin` eklemek kapıyı kapatmaz, açar. Kök çözüm: `SECURITY DEFINER` + pinned `search_path` bir BEFORE INSERT/UPDATE trigger'ı. `select ... into` eşleşen satır yoksa hata atmaz, değişkeni NULL bırakır → `coalesce(..., false)` olmadan guard sessizce devre dışı kalır.
* **[2026-07-25 — Apple: native Sign in with Apple'da `.p8` gerekmez]:** Supabase Apple provider'ı için Services ID / Team ID / Key ID / `.p8` **yalnız web (OAuth) akışının** gereğidir. Uygulama `getAppleIDCredential` → `signInWithIdToken` ile yerel akış kullandığından **Client IDs alanına bundle ID yazmak yeterlidir**. Ayrıca Codemagic `ios_signing`, provisioning profile'ı her build'de Apple'dan çekmez; **Teams → Code signing identities altındaki saklanan kopyayı** kullanır — Apple'da profili yenilemek tek başına build'i düzeltmez, saklanan kopya da silinip yeniden çekilmelidir.
* **[2026-06-15 — Mimari]:** Riverpod sentezi seçildi: HTML planının Riverpod'u + mimari skill'in MVVM katmanları birleştirildi (Notifier=ViewModel, provider grafiği=DI).
* **[2026-06-15 — Backend]:** Supabase (BaaS); ayrı sunucu yok. HTML'deki "Dio/REST" Supabase ile değiştirildi.
* **[2026-06-15 — DB: Isar → drift]:** Isar 3.x ve isar_community generator'ı `source_gen`/`analyzer 12`'yi desteklemiyor (freezed 3 + json_serializable 6.14 ile çakışma). drift (SQL+FTS5, güncel analyzer) ile değiştirildi. Repository soyutlaması mimariyi korudu.
* **[2026-06-15 — KRİTİK ortam/Anti-Pattern]:** Proje yolu `OneDrive\Masaüstü\...` içindeki **`ü` karakteri `gen_snapshot`'ın AOT yazmasını kırıyor** (build_runner exit 78 "Unable to write .aot"). **ÇÖZÜM:** ASCII junction `C:\kdm` → proje; codegen/release derlemelerini `cd C:\kdm` ile çalıştır. Kalıcı çözüm: projeyi ASCII, OneDrive-dışı bir yola taşı (örn. `C:\Users\yiit5\dev\kurandakimesaj`).
* **[2026-06-15 — Ortam]:** `flutter build bundle/apk` Android SDK gerektiriyor ("Android SDK could not be found"); cihaz/emülatör testi için kurulmalı.
* **[2026-06-15 — Riskler]:** ffmpeg_kit_flutter retired (Sprint 3'te sunucu-taraf render); flutter_qiblah fallback (sensörsüz bearing hesabı); just_audio arka plan (audio_service).
* **[2026-06-15 — Ortam ÇÖZÜLDÜ]:** Proje `Desktop\kurandaki mesaj flutter\kurandakimesaj` (ASCII) yoluna taşındı → `ü`/junction sorunu bitti; `dart analyze`+`build_runner`+`flutter build` doğrudan çalışıyor. Android SDK kuruldu; uygulama Samsung SM S731B'de `flutter run` ile açıldı.
* **[2026-06-15 — Android yapılandırma]:** Plugin'ler için `android/app/build.gradle.kts`: `minSdk 23`, `isCoreLibraryDesugaringEnabled = true` + `desugar_jdk_libs:2.1.4` (flutter_local_notifications 22 şart), `multiDexEnabled`. Manifest izinleri: INTERNET, ACCESS_FINE/COARSE_LOCATION, POST_NOTIFICATIONS, VIBRATE.
* **[2026-06-15 — Paket API tuzakları (bkz. tasks/lessons.md)]:** Riverpod 3: `AsyncValue.value` (`.valueOrNull` yok), `StateProvider` → `flutter_riverpod/legacy.dart`. flutter_local_notifications 22 tamamen named param. share_plus 13 `SharePlus.instance.share(ShareParams())`. `intl`'in `TextDirection`'ı Flutter ile çakışır → `hide TextDirection`. **RevenueCat paketi (`purchases_flutter`) EKLENMEDİ** — PRO yerel `proProvider` (prefs) ile soyutlandı; gerçek hak `revenuecat-webhook` → `profiles.is_pro` ile gelecek.
* **[2026-06-15 — Backend (kod tamam)]:** `supabase/` (config + 2 migration + 5 Edge Function). Şema 15 tablo, hepsinde RLS default-deny. İstemci `lib/data/backend_repositories.dart` — Supabase yoksa null-guard → yerel fallback. **AI atlandı** (kullanıcı kararı; yerel öneri kaldı). Deploy talimatı: `supabase/README.md`. Canlı projeye HENÜZ deploy edilmedi (CLI/Docker yok).
* **[2026-06-16 — Kalıcılık politikası]:** "X nerede yaşamalı?" karar matrisi `docs/persistence-policy.md`'de kilitlendi. Render işleri **prefs**'te (drift değil) — drift `onUpgrade`'in yıkıcı sıfırlama riskinden kaçınmak için. drift'te seed ≠ kullanıcı verisi; `_userTables` (collections/memorizations/juzProgress/dhikrCounters) şema yükseltmede KORUNUR. Kullanıcı tablosuna sütun eklersen açık `m.addColumn` şart (IF NOT EXISTS eski tabloyu olduğu gibi bırakır).
* **[2026-06-16 — Supabase erişim merkezileştirme]:** Ham `Supabase.instance` yalnızca `supabaseClientProvider` (Provider<SupabaseClient?>, try-catch null) içinde; her yer `SupabaseGateway` (`isAvailable`/`isSignedIn`) veya enjekte istemci üzerinden gider. `grep "Supabase.instance"` → tam 1 sonuç (doğrulandı). Backend yoksa onurlu düşüş (honest degradation), assert/crash yok.
* **[2026-06-16 — Akış dürüstlüğü (ürün ilkesi)]:** Topluluk akışında SAHTE yazar/etkileşim YOK. Seed içeriği açıkça "Günün Seçkileri" (curation, likeCount=null, yerel beğeni); yalnızca bulut gönderileri gerçek yazar/sayı gösterir. Boş bulut durumu = dürüst boş + "İlk ayetini paylaş" CTA → şablonlar.
* **[2026-06-16 — Doğrulama disiplini]:** Bug/failing-test yokken `/investigate` (kök-neden hata ayıklayıcı, "düzeltme yok ki kök-neden yok") uygulanmaz; doğru doğrulama `dart analyze` + `flutter test`'tir (Flutter PATH'te değil → `~/flutter/bin/dart|flutter`).
* **[2026-06-15 — GateGuard]:** ECC fact-forcing gate her yeni Write/Edit/Bash'i ilk denemede bloklar; retry geçer. Kapatmak için kullanıcının `ECC_GATEGUARD=off` env'i (yeni oturumda). Agent kendi onay kapısını kapatamaz (auto-mode bypass engeli).
* **[2026-06-20 — PRO kaldırma kararı]:** RevenueCat entegrasyonu ertelendi — tüm PRO kapıları (ProController/ProUpsell/isPro) kaldırıldı. Her özellik ücretsiz. Supabase `is_pro` kolonu backend'de kaldı (ileride yeniden aktive edilebilir) ama client-side gating tamamen silindi.
* **[2026-06-20 — Video render mimarisi]:** ffmpeg_kit retired → Bu sprint RepaintBoundary+toImage() PNG çıktı (cihaz üzeri, ücretsiz). Gerçek video render (sunucu taraflı) ertelenmiş TODO olarak kaldı. Studio çıktısı = PNG paylaşım/Storage yükleme.
* **[2026-06-20 — STT seçimi]:** speech_to_text paketi (cihaz üzeri, ücretsiz, tr-TR) seçildi. Groq/Whisper API araştırıldı ama ücretsiz cihaz STT + Supabase Edge LLM kombinasyonu yeterli kabul edildi.
* **[2026-06-20 — Altın fiyatı API]:** TCMB XML'de doğrudan gram altın fiyatı yok (sadece döviz kuru). Çözüm: kullanıcı manuel giriş + SharedPrefs 24 saatlik cache. collectapi/goldapi.io ücretli olduğundan atlandı.
* **[2026-06-20 — Drift schemaVersion 3]:** ReadingEvents (okuma istatistikleri) + FavoriteSurahs (favori sure) tabloları eklendi. onUpgrade `from < 3` bloğu ile güvenli migration. _userTables koruması devam ediyor.
* **[2026-06-20 — NotificationService ID aralıkları]:** Dua bildirimleri ID 1-10, İslami takvim bildirimleri ID 100-199 (1 gün önce) + 150-199 (gün sabahı). Çakışma riski elimine edildi.
* **[2026-06-23 — Esma grid taşma fix (/investigate)]:** Esmaü'l-Hüsna kartları 2-satırlık anlam metninde "BOTTOM OVERFLOWED BY 12px" taşıyordu. Kök neden: `esma_screen.dart` Column'unda hiçbir esnek çocuk yok — sabit yükseklikli içerik (Arapça `height:1.9` 57dp + isim + 2-satır anlam ≈ 122dp) + AppCard `all(18)` padding, `childAspectRatio:1.15`'in verdiği hücre yüksekliğini (~160dp) aşıyordu; dar ekranda Arapça da sarıp daha çok taşıyordu. Fix: Arapça `maxLines:1`+`height:1.35`, isim `maxLines:1`, anlam `Flexible` içine (daralınca ellipsize), `childAspectRatio:1.15→0.95`. Regresyon testi: `test/esma_overflow_test.dart` (360dp dar ekran, `takeException()==null`). Kanıt: fix öncesi 40px taşma → sonrası geçer. NOT: `ai_assistant_screen.dart`'ta 3 önceden var olan `speech_to_text` deprecation info'su kaldı (ayrı iş).
* **[2026-06-20 — follows tablosu graceful degradation]:** follows Supabase tablosu Flutter client'tan DDL ile oluşturulamaz. SQL `docs/supabase/follows_migration.sql`'de hazır, manuel dashboard deploy gerekli. Tablo olmadan takip butonu try/catch ile sessizce devre dışı kalır, crash yok.
* **[2026-06-23 — Ayet tanıma model seçimi]:** Ses→**Groq Whisper Large v3 Turbo** (ücretsiz 2.000/gün, $0.04/saat); Görsel→**Gemini 2.0 Flash** (cömert ücretsiz tier, Arapça vision OCR). İkisi de yalnız **Arapça metin çıkarır**; ayet adı+meal HER ZAMAN yerel FTS/matcher'dan gelir (LLM uydurması engellenir). Tarteel Kur'an-fine-tuned Whisper, tilavet isabeti yetersiz kalırsa yükseltme yolu olarak belgelendi.
* **[2026-06-23 — FTS5 yerine in-memory matcher]:** 6236 kısa ayet için drift FTS5 codegen karmaşıklığı yerine bellekte `QuranMatcher` (lazy yüklenir, token-coverage + en uzun bitişik token dizisi bonusu) seçildi — ASR/OCR gürültüsüne daha sağlam, tam test edilebilir, codegen riski yok. Normalized metin DB'de TUTULMAZ; matcher yüklerken `ArabicNormalizer` ile hesaplar (tek doğru kaynak).
* **[2026-06-23 — Tam Kur'an asset + schemaVersion 4]:** Tüm Kur'an (6236 ayet) `assets/quran/quran_full.json` olarak gömüldü (`tool/fetch_quran.dart`, sıfır-bağımlılıklı dart:io, AlQuran Cloud). `QuranFullAyahs` seed tablosu; onUpgrade non-user tabloları zaten yeniden oluşturduğu için _userTables koruması bozulmadan migrate olur.
* **[2026-06-23 — Edge Function key güvenliği]:** `GROQ_API_KEY`/`GEMINI_API_KEY` Supabase secrets'ta (Deno.env), istemcide DEĞİL (Rule #1). AppConfig'te `String.fromEnvironment` iskeleti yalnız prototip içindir. NOT: key'ler bir kez sohbette düz metin paylaşıldı → kullanıcıya rotate önerildi.
* **[2026-06-23 — AGP 9 / newDsl build çökmesi ÇÖZÜLDÜ]:** `flutter run` Android'de patlıyordu: "Starting AGP 9+, only the new DSL interface will be read... build failure when applying the Flutter Gradle plugin". Kök neden: `android/settings.gradle.kts` AGP **9.0.1** kullanıyordu ama Flutter 3.44.2'nin Gradle plugin'i eski DSL ile uygulanıyor; `gradle.properties`'teki `android.newDsl=false` AGP 9'da bu çakışmayı çözmüyor. ÇÖZÜM: AGP 9.0.1→**8.11.1**, Kotlin plugin 2.3.20→**2.2.20** (settings.gradle.kts). Sonra iki engel daha çıktı: (1) build-tools 35.0.0 "Failed to install" → klasör vardı ama SDK **lisansı kabul edilmemişti**; `sdkmanager --licenses` ile kabul edildi (`JAVA_HOME`=Android Studio jbr şart, yoksa "JAVA_HOME not set"). (2) `speech_to_text` 6.6.2 Kotlin derlemesi "Unresolved reference 'Registrar'" — paket kaldırılmış Flutter **v1 embedding** API'si kullanıyordu → `flutter pub upgrade speech_to_text --major-versions` ile **7.4.0**'a yükseltildi. Doğrulama: `flutter clean` + `flutter build apk --debug` → `√ Built app-debug.apk` (192MB). Kalan zararsız uyarı: bazı pluginlere "Built-in Kotlin'e geç" önerisi (build'i etkilemiyor). NOT: `record` zaten 7.x'te (aşağıdaki giriş), bu da AGP 8.x ile tutarlı. `dart-build-resolver` agent + manuel doğrulama ile çözüldü.
* **[2026-06-23 — record paketi federated sürüm tuzağı]:** `record` 5.2.1 KIRIK ara noktadır: `record_platform_interface` 1.6.0 (`startStream`/yeni `hasPermission`) çekiyor ama `record_linux` 1.x hattında bunu implement eden sürüm YOK (0.7.2 takılı) → Android dahil HER platformda `kernel_snapshot` derleme hatası ("RecordLinux missing startStream"). ÇÖZÜM: `record: ^7.1.0` (interface 2.x + record_linux 2.1.0, tutarlı set). `AudioRecorder` API'si (start/stop/hasPermission/isRecording/dispose, RecordConfig+AudioEncoder.aacLc) 5.x↔7.x aynı; servis değişmeden derlendi. `record`'u 5.x'e DÜŞÜRME.

* **[2026-06-23 — Inconsistent JVM-target build hatası ÇÖZÜLDÜ (/investigate)]:** `flutter run` → `:receive_sharing_intent:compileDebugKotlin` "Inconsistent JVM-target compatibility (Java 1.8 / Kotlin 21)" ile patlıyordu. Kök neden: plugin'ler Java hedefini compileOptions ile eski sürüme (1.8/11) sabitliyor ama Kotlin hedefini sabitlemiyor; KGP 2.2.20 onu Gradle JDK'sına (Android Studio jbr **21**) göre belirliyor → modül içi uyumsuzluk. `:app`'in 17/17 ayarı plugin (subproject) modüllerine yayılmıyor. ÇÖZÜM (`android/build.gradle.kts` subprojects bloğu): Java'ya DOKUNMADAN her library modülünün mevcut `JavaCompile.targetCompatibility`'sini okuyup Kotlin `jvmTarget`'ı ona EŞİTLE. Tuzaklar: (1) Kotlin'i sabit 17'ye zorlamak audio_session'da (11/11 iken) yeni uyumsuzluk yarattı. (2) Java task'ına doğrudan source/targetCompatibility yazmak javac'ı `--release` moduna sokup android.jar bootclasspath'ini düşürdü → flutter_compass'ta "package Surface does not exist" (97 hata). (3) `afterEvaluate`'i `evaluationDependsOn(":app")` ile aynı blokta çağırmak "project already evaluated" hatası verdi → ayrı `plugins.withId("com.android.library"){ afterEvaluate{} }` bloğuna taşındı (AGP compileOptions'ı uyguladıktan SONRA çalışır). Doğrulama: `flutter build apk --debug` → `√ Built app-debug.apk`. Kalan zararsız: KGP built-in Kotlin önerisi + Java 8 "obsolete" deprecation uyarıları.

* **[2026-06-23 — "Supabase bağlantısı yok" = run-config eksiği (/investigate)]:** Ayet Tanı ekranı "Bu özellik için internet/Supabase bağlantısı gereklidir" gösteriyordu. KOD HATASI DEĞİL — tasarım gereği. Zincir: `ayah_recognition_screen.dart:48` `_hasBackend = supabaseClientProvider != null`; provider (`backend_repositories.dart:5`) `Supabase.instance.client`'ı dener, başlatılmamışsa null. `app.dart:28` `bootstrap()` yalnız `AppConfig.hasSupabase` ise `Supabase.initialize()` çağırır; `hasSupabase` = `SUPABASE_URL`+`SUPABASE_ANON_KEY` dolu (`app_config.dart`), bunlar `String.fromEnvironment` = derleme zamanı `--dart-define`. `flutter run` bunlar olmadan çalıştırıldığından ikisi de boş → init atlanır → uyarı. ÇÖZÜM: `env.json` (gitignore'lu) + `flutter run --dart-define-from-file=env.json`. Şablon: `env.example.json` (commit'li). Değerler Supabase Dashboard → Settings → API (Project URL + anon public). NOT: anon key public/RLS-korumalı, istemcide olması normal; tehlikeli olan service_role + Groq/Gemini key'leri (onlar Edge Function Deno.env'de, Rule #4).

* **[2026-06-23 — Ayet tanıma "yanlış ayet" fix + AI chat entegrasyonu (/investigate)]:** İKİ iş. (1) BUG: tanıma çalışıyordu (Edge Function'lar deploy + secret'lar tamam, Arapça metin çıkıyor) ama `QuranMatcher` alakasız sure:ayet getiriyordu. Kök neden: skorlama `coverage(0..1) + run(0..0.3)` idi — IDF (kelime nadirliği) ve precision (aday uzunluk normalizasyonu) yoktu; gürültülü ASR/OCR'da query'nin tüm kelimelerini DAĞINIK içeren UZUN ayet coverage=1.0 ile doğru kısa ayeti geçiyordu, ayrıca `الله/من/في` gibi yaygın kelimeler nadir ayırt edici kelimelerle aynı ağırlıktaydı. ÇÖZÜM (`lib/data/quran_matcher.dart`): skorlama → **IDF-ağırlıklı F1 (recall+precision) + bitişik ifade bonusu (0..0.5)**. Constructor df→idf hesaplar; sözlükte olmayan token (ASR gürültüsü) 0 ağırlık alır. Regresyon: `test/quran_matcher_test.dart`'a 2 test (gürültülü-uzun-dağınık senaryo + nadir kelime baskınlığı); eski skorlama bunlarda kaybediyordu. 28/28 test geçer. (2) ENTEGRASYON (kullanıcı "direkt chate" istedi): ayrı `ayah_recognition_screen.dart` SİLİNDİ; tanıma `ai_assistant_screen.dart`'a gömüldü — "Ayeti oku" (Arapça tilavet kaydı) + "Görselden tanı" butonları, sonuç RTL/Amiri ayet balonu (`_RecognitionBubble`/`_MatchTile`) olarak akışa eklenir. `_Msg` modeli `arabic`+`matches` taşır. Türkçe STT mikrofonu (konu yazma) korundu — iki mik tooltip ile ayrıştırıldı ("Sesli yaz (konu)" vs "Ayeti oku"). Router: `/ayah-recognition` artık `AiAssistantScreen(initialImagePath:)` render eder → paylaş-intent (Instagram→Paylaş) akışı korundu, `app.dart` değişmeden çalışır. `flutter analyze` temiz (yalnız 3 önceden var olan speech_to_text deprecation info'su). NOT: tanıma hâlâ "yanlış" gelirse sebep ASR/OCR model kalitesi (Whisper tilavet isabeti) olabilir — Tarteel fine-tuned Whisper yükseltme yolu açık.

* **[2026-06-25 — Render-retry ölü-kod gerekçesiyle ertelendi]:** "(P3) Render hata durumu + tekrar dene" TODO'su backend render hattına BAĞLI olarak ertelendi. Kök analiz: `RenderJob(...)` uygulama kodunda hiç oluşturulmuyor, `renderJobsProvider.notifier).add` çağrısı yok — studio 2026-06-20'de bilinçle PNG-export'a geçince (`ffmpeg_kit retired`) render kuyruğu uykuya daldı. Kuyruğu besleyen producer (render-trigger/status Edge) deploy edilmeden retry UI'si test EDİLEMEZ ölü koddur (CLAUDE.md kural 5: over-engineering yapma + "staff engineer onaylar mı?"). `RenderStatus.failed` zaten enum + ikon/etiketle ele alınıyor; retry, backend render hattıyla BİRLİKTE canlanmalı.
* **[2026-06-25 — Sohbet baloncuk token'ı]:** İki sohbet yüzeyi (Mesajlar elle r14 simetrik · AI asistan+ayet-tanıma elle `BorderRadius.only` 16/4 asimetrik) `AppRadii.chatBubble(mine:)` tek yardımcısına bağlandı. Yeni `bubble`(16)/`bubbleTail`(4) token'ları. Kuyruk gönderen tarafında alt-yakın köşede sivrilir. Regresyon: `test/chat_bubble_radii_test.dart`. Yeni sohbet yüzeyleri elle BorderRadius yazmamalı.
* **[2026-06-29 — Android applicationId → com.kurandakimesaj.app]:** Android paket adı iOS ile eşitlendi (`com.example.kurandakimesaj` → `com.kurandakimesaj.app`). Değişen: `android/app/build.gradle.kts` (`namespace` + `applicationId`), MainActivity.kt yeni pakete taşındı (`kotlin/com/kurandakimesaj/app/MainActivity.kt`, eski `com/example/` klasörü silindi). Manifest `.MainActivity` görelatif → değişmedi. `grep com.example.kurandakimesaj` → 0 (gradle cache hariç). **Google OAuth Android client artık `com.kurandakimesaj.app` + debug SHA-1 ile kurulmalı.** Sonraki build öncesi `flutter clean` önerilir (namespace değişti).
* **[2026-06-29 — Yayın hesapları (kullanıcı bildirdi)]:** 🍎 Apple App Store yayın = `erenasanpp@icloud.com` (Developer Program). 🤖 Google Play Console yayın = `erenasanpp@gmail.com`. ☁️ Google Cloud Console (OAuth/Google ile giriş + API'ler) = `kurandakimesaj55@gmail.com`. **Üçünün farklı olması sorun DEĞİL** — bağımsız sistemler, eşleşme gerekmez. Google girişi Cloud proje sahibine değil OAuth client config'ine (package+SHA-1) + Supabase provider'a bağlı. Riskler: (1) üç hesaba da erişimi koru — Cloud hesabı kaybı tüm kullanıcılarda girişi bozar; (2) üretimde Play App Signing SHA-1 da Android OAuth client'a eklenmeli; (3) iOS'ta Google girişi sunulursa Apple Sign in with Apple'ı zorunlu kılar (Guideline 4.8). Detay: `STORE_SUBMISSION.md` → "Yayın Hesapları".
* **[2026-06-29 — Google girişi "çalışmıyor" = config eksiği (/investigate)]:** Ekranda "Google ile giriş başarısız. Yapılandırmayı kontrol edin." KOD HATASI DEĞİL. Kök neden: `env.json` yalnız `SUPABASE_URL`+`SUPABASE_ANON_KEY` içeriyor; `GOOGLE_WEB_CLIENT_ID` yok → `AppConfig.googleWebClientId` boş → `services.dart:164` `signInWithGoogle()` ilk satırda `StateError` fırlatıyor. Kod (google_sign_in 7.2 native akış → `signInWithIdToken`) tam ve doğru. Buton `hasGoogleSignIn` ile gizlenmiyor (her zaman görünür). ÇÖZÜM = kullanıcı kurulumu: (1) Google Cloud Console → OAuth: **Web client** (Supabase'e + `serverClientId`) + **Android client** (package `com.example.kurandakimesaj`, debug SHA-1 `5B:7E:12:3A:B1:97:75:82:2A:1D:34:04:FB:7A:2A:2E:17:5B:FA:A4`). (2) Supabase Dashboard → Auth → Providers → Google etkin + Web client ID/secret. (3) `env.json`'a `GOOGLE_WEB_CLIENT_ID` ekle. NOT: applicationId hâlâ `com.example.*` → yayın öncesi gerçek paket adına çevrilmeli (SHA-1 de değişir).
* **[2026-06-29 — AI özelliği TAMAMEN kaldırıldı (kullanıcı kararı)]:** Hem AI Asistan sohbeti hem ayet/sure tanıma silindi. **Silinen dosyalar:** `lib/features/ai/` (ai_assistant_screen), `lib/data/quran_matcher.dart`, `lib/domain/arabic_normalizer.dart`, `lib/data/services/quran_recognition_service.dart`, `assets/quran/quran_full.json` (+klasör), `tool/fetch_quran.dart`, `supabase/functions/quran-{vision,recognize}/`, `test/{quran_matcher,arabic_normalizer}_test.dart`. **Düzenlenen:** router (/ai-assistant + /ayah-recognition rotaları), repositories (quranRecognitionServiceProvider), content_repository (matchRecitation + _ensureMatcher + interface decl), feature_catalog (24→23 özellik, İçerik 3'e indi), home_screens (AI FAB + "AI ile Üret" sheet öğesi), app.dart (receive_sharing_intent paylaş-intent mantığı tümü → KuranApp tekrar ConsumerWidget), seed_service (_seedQuranFull + dart:convert/rootBundle import), AndroidManifest (RECORD_AUDIO izni + ACTION_SEND image/* intent-filter). **Drift:** schemaVersion **4→5**, `QuranFullAyahs` tablosu kaldırıldı + onUpgrade'e `if(from<5) deleteTable('quran_full_ayahs')` (eski cihazda yetim tablo temizliği), build_runner yeniden üretildi. **Paketler kaldırıldı:** `speech_to_text`, `record`, `receive_sharing_intent` (file_picker/image_picker KALDI — studio/community kullanıyor). `dart analyze` 0 sorun, **18/18 test**. **Canlı backend temizliği YAPILDI (2026-06-29):** `quran-vision` + `quran-recognize` Edge Functions silindi, `GROQ_API_KEY`/`GEMINI_API_KEY` secret'ları unset edildi (REF uytqxgcbohdlwwxemgyy).
* **[2026-06-30 — Ayet Bulucu (görsel/bağlantıdan ayet tanıma) GERİ EKLENDİ — sunucu-merkezli]:** 2026-06-29'da kaldırılan tanıma özelliği, kullanıcı isteğiyle daha dar kapsamda (sadece görsel/bağlantı → ayet; sohbet/sesli tilavet YOK) yeniden inşa edildi. **Önceki tasarımdan kritik fark:** veri seti + eşleştirme artık istemcide DEĞİL, tamamen Supabase Edge Function'da (sunucuda). İstemciye 2.3MB asset paketlenMEZ → uygulama şişmez, tekrar-silme maliyeti düşük. **Yeni dosyalar:** `supabase/functions/ayah-finder/{index.ts, matcher.ts, quran_data.ts, link_resolver.ts(Faz2), matcher.test.ts}`, `tool/fetch_quran.dart` (yeniden — quran_data.ts üretir, 6236 ayet), `lib/features/ayah_finder/{ayah_finder_screen.dart, ayah_finder_controller.dart}`. **Düzenlenen:** `lib/domain/models.dart` (AyahMatch+AyahFinderResult, **freezed DEĞİL** — dosya stili düz @immutable+fromJson), `lib/data/backend_repositories.dart` (IAyahFinderRepository+AyahFinderRepository+provider, RenderRepository kalıbı), router (+/ayah-finder), feature_catalog (23→24, İçerik 4), home_screens (+Ayet Bul sheet öğesi). **AI motoru:** Gemini 2.0 Flash görsel (Edge'de, `GEMINI_API_KEY` secret). LLM YALNIZ Arapça metni çıkarır, sure:ayet TAHMİN ETMEZ → otoriter `quran_data.ts` IDF-F1 matcher (silinen quran_matcher mantığının TS portu) sure:ayet+Diyanet meali döner. Belirsizlikte tek yanlış ayet yerine aday listesi (status=ambiguous). **Doğrulandı:** `flutter analyze` 0, **18/18 test**, matcher elle doğrulandı. **MANUEL ADIMLAR (Faz 1 için):** `supabase functions deploy ayah-finder` + `supabase secrets set GEMINI_API_KEY=...` (secret koda gömülmez). **Fazlama:** Faz 1 galeri (TAMAM) · **Faz 2 bağlantı (TAMAM — 2026-06-30):** `link_resolver.ts` og:image/twitter:image çeker, `index.ts` ortak görsel→eşleştirme yoluna bağlandı (postUrl→resolve→fetch→base64→vision); IG giriş duvarı→`link_unresolved` + UI fallback. İstemci `fromUrl` zaten bağlıydı. · **Faz 3 paylaş menüsü (TAMAM — 2026-06-30):** `receive_sharing_intent: ^1.8.1` geri eklendi; `app.dart` `getMediaStream()/getInitialMedia()` dinleyicisi paylaşılan görsel/URL'yi `sharedAyahInputProvider` (Notifier — Riverpod 3'te StateProvider legacy olduğu için) üzerinden AyahFinderScreen'e aktarır; `initState` bir kez tüketir. **Android** AndroidManifest'e ACTION_SEND `image/*`+`text/*` intent-filter eklendi (çalışır). **iOS Share Extension macOS/Xcode manuel** — `ios/SHARE_EXTENSION_SETUP.md` (App Group `group.com.kurandakimesaj.app`). Doğrulandı: `flutter analyze` 0, 19/19 test.
* **[2026-06-30 — Tam Kur'an OFFLINE gömüldü (okuma ekranı eksik ayet fix)]:** ŞİKAYET: "Kuran ayetleri eksik, bütün ayetler yok". TEŞHİS: AlQuran Cloud API (`api.alquran.cloud`) + istemci (`QuranApi`+dio, `repositories.dart:148`) + lazy-fetch (`content_repository.dart:ayahsForSurah`) **SAĞLAM** — canlı test code 200, doğru ayet sayıları, INTERNET izni var. KÖK NEDEN: okuma ekranı tam Kur'an'ı paketlemiyordu; `kAyahSeed` yalnız **77 küratörlü ayet** içeriyordu, geri kalan ~6159 ayet yalnız online + sure-sure lazy-fetch'le geliyordu → offline'da %99 eksik, offline-first kuralına aykırı. ÇÖZÜM (kullanıcı onayı=offline göm): tam Kur'an `supabase/functions/ayah-finder/quran_data.ts`'te zaten vardı → `tmp/gen_quran.py` ile (ağsız, repodaki TS verisinden) `assets/quran/quran_full.json` üretildi (6236 ayet, 2.48MB). **Düzenlenen:** pubspec (assets/quran/ kaydı), `seed_service.dart` (`_seedAyahs` artık `rootBundle.loadString`+jsonDecode ile asset'ten 6236 satır batch insert; `dart:convert`+`flutter/services` import), `seed_data.dart` (`kAyahSeed` 77-ayet listesi SİLİNDİ — tek doğru kaynak asset), `app_database.dart` (schemaVersion **5→6** → onUpgrade seed `ayahs` tablosunu silip yeniden seed'ler, _userTables korunur, build_runner GEREKMEDİ — sadece int). Lazy-fetch güvenlik ağı olarak KALDI (cache tamsa tetiklenmez). Yeni test `test/quran_full_asset_test.dart` (asset bütünlüğü: 6236/114/Fatiha7/Bakara286, boş ar/meal yok). **Doğrulandı:** `flutter analyze` 0 sorun, **19/19 test** geçti. Deploy gerekmez (asset uygulamayla paketli). NOT: meal kaynağı tr.diyanet (onaylı); Arapça uthmani (ۛۖٱ ortografisi — kAyahSeed'den farklı hat ama ikisi de geçerli).
* **[2026-06-30 — Ana ekran widget mimarisi]:** Telefon ana ekran widget'ları için **`home_widget`** köprü deseni: Flutter veriyi yazar (`HomeWidget.saveWidgetData`), native widget okur. Tek veri kaynağı `WidgetSyncService` — namaz/hicri/ayet'i anahtar-değer yazıp `updateWidget(qualifiedAndroidName, iOSName)` çağırır. Senkron ANA SAYFA açıldıkça (`HomeScreen.build` → `_syncHomeWidgets` → `ref.listen(prayerControllerProvider/dailyAyahProvider)`) + arka planda `workmanager` (sadece ezan). **Bilinçli kısıtlar:** (1) RemoteViews/WidgetKit canlı saniye sayacı çalıştıramaz → widget "sıradaki vakit + saat" gösterir, geri sayım yalnız uygulama içi kartta. (2) **Ayet widget'ında Arapça hat YOK, sadece Türkçe meal+kaynak** — Amiri Quran fontu native widget'a paketli değil, domain kuralı #2 gereği yanlış fontla mushaf hattı render edilmez. (3) Arka plan yenileme yalnız **ezan** (zamana duyarlı); ayet günde bir değişir, drift DB'yi background isolate'te açmaktan kaçınmak için uygulama-açılış senkronu yeterli. (4) Background isolate `geolocator` çağırmaz — konum `PrayerController.build`'de prefs'e yazılır (`kLastLat/Lng/LocLabelKey`), background oradan okur (arka plan konum izni gerekmez). **iOS = manuel:** WidgetKit extension target'ı `.pbxproj`'a elle eklenemez + Windows'ta iOS derlenemez → Swift/plist/entitlement dosyaları hazır, target oluşturma + App Group (`group.com.kurandakimesaj.app`) + iOS workmanager BGTaskScheduler adımları `ios/KuranWidget/SETUP.md`'de. Deep-link: `homeWidget://prayer|ayah` (Runner Info.plist URL şeması) → `app.dart` `KuranApp` (ConsumerStatefulWidget, `HomeWidget.widgetClicked`+`initiallyLaunchedFromHomeWidget`) → `routerProvider.go('/prayer'|'/daily-ayah')`. `dart analyze` 0, 18/18 test, Android debug APK ✓.
* **[2026-06-30 — receive_sharing_intent 1.8.1'e pinlendi (build kırılması fix)]:** `flutter run` Android build'i `:receive_sharing_intent:generateDebugRFile` → "Failed to find target with hash string 'android-37'" + BUILD FAILED ile çöküyordu. Kök neden: pubspec `^1.8.1` caret kısıtı **1.9.0**'ı çekmişti; 1.9.0 `android/build.gradle` `compileSdk 37` zorluyor (1.8.1 compileSdk 34). API 37 henüz stable DEĞİL — SDK'da yalnız preview `android-37.0` var, Gradle stable `android-37` hash'ini arıyor → bulamıyor. ÇÖZÜM: pubspec'te `receive_sharing_intent: 1.8.1` (caret kaldırıldı, minor bump 37'ye dönmesin) → `pub get` 1.9.0→1.8.1 downgrade → `flutter run` başarılı, uygulama SM S731B'de açıldı. NOT: proje compileSdk = `flutter.compileSdkVersion` (=36); 37'ye çıkarmak da çözmezdi çünkü stable android-37 paketi yok.
* **[2026-07-01 — Profil isim düzenleme aralıklı çökme fix (/investigate)]:** ŞİKAYET: "profilde isim değiştirirken hata alıyorum". Cihaz reprosu: kırmızı ekran `framework.dart:6268 InheritedElement.debugDeactivated: '_dependents.isEmpty' is not true`. KÖK NEDEN: `_showEditNameDialog` (community_screens.dart) async helper'da `TextEditingController`'ı `await showDialog` SONRASI manuel `dispose()` ediyordu → dialog/EditableText hâlâ teardown olurken controller erken yok ediliyor, InheritedElement deactivation sırasını bozuyor. **DEBUG-ONLY assertion** (release'de `debugDeactivated` çalışmaz → çökmez); DB güncellemesi zaten başarılıydı, çökme `_load()` rebuild'inde ARALIKLI (~2/3) oluyordu. Yanlış hipotez (elendi): tema `KeyedSubtree`'si — kaldırınca da çöktü, ilgisiz. ÇÖZÜM (kanonik): controller'ı `_EditNameDialog` StatefulWidget'ına sahiplendirdim; `State.dispose()` element tam deactive olduktan SONRA çalıştığı için yarış yok. Ayrıca updateDisplayName try/catch + SnackBar + mounted guard. Teşhis: geçici `FlutterError.onError`+`debugPrint('GERR:')` (Impeller/Vulkan debug'da assertion logcat'e düşmüyordu; `flutter run` da alınamadı → framework.dart:6268'i SDK'dan okuyup teşhis edildi). Doğrulama: 5 gerçek isim değişimi, 0 çökme (eski kod 2/3 çöküyordu). `dart analyze` 0, 20/20 test. **DERS:** async helper'da `await showDialog` sonrası `TextEditingController.dispose()` çağırma — controller'ı dialog'un kendi StatefulWidget State'inde sahiplendir.
* **[2026-07-01 — Koyu/açık tema mimarisi (global brightness-duyarlı palet)]:** Tasarım sistemi `AppColors.x` sabitlerini kod tabanında DOĞRUDAN kullanıyordu (`Theme.of(context)` değil, ~500 ref). Gerçek açık tema için `AppColors` brightness-duyarlı facade'e çevrildi: `static Color get x => _light ? ... : ...`, tek global `_b` (`AppColors.brightness` setter). Çağrı yerleri değişmedi. **Anlamsal rol ayrımı ŞART:** `emerald950` hem scaffold-bg (döner) hem "gold üstü koyu metin" (dönmemeli) olarak çakışıyordu → yeni SABİT `onGold` ile buton/chip/FAB foreground'ları ayrıldı; gold-METİN için `goldInk` (açık temada koyulaşır). Hero gradyanı açık temada da AÇIK yapıldı (krem) → hero üstü metin de döner, ekstra `onHero` denetimi gerekmedi. **İKİ TUZAK:** (1) `MaterialApp.builder`'da `Theme.of(context).brightness` YANLIŞ değer döndürür → brightness themeMode+`MediaQuery.platformBrightnessOf` ile hesaplanıp global'e yazılmalı. (2) `const` widget'lar global palet değişince yeniden build OLMAZ (canlı temada eski renkte kalırlar) → builder child'ı `KeyedSubtree(key:ValueKey(isDark))` ile sarıldı, tema değişince tüm ağaç yeniden build olur; go_router delegate mevcut route'u koruduğu için navigasyon kaybolmaz. Uygulama RELAUNCH'ta global build'den önce set edildiği için const dahil her şey doğru; sorun yalnız CANLI toggle'daydı, KeyedSubtree çözdü. Getter'lar const olmadığından çok az const kırıldı (yalnız gerçekten `const` bağlamındaki 2 yer + `const AppBarTheme` — gold ailesi const kaldığı için 41 gold-const yeri korundu).
* **[2026-07-01 — Google girişi "başarısız" = başarılı girişin pop bug'ıyla yanlış etiketlenmesi (/investigate)]:** Kullanıcı "google bağlantı sorunları" bildirdi. Cihaz logcat'i (taze, fix öncesi reproduce) gösterdi: `signInWithGoogle()` BAŞARIYLA tamamlanıyor (CredentialManager `CREDENTIALS_RECEIVED`, `signInWithIdToken` OK, oturum kalıcı → Profil `yiit55400`), AMA hemen sonraki `context.pop()` `GoError: There is nothing to pop` fırlatıyor → geniş `catch` onu "Google ile giriş başarısız" sanıyor. Kök neden navigasyon, Google config DEĞİL. Tetikleyici: `/auth`'a `context.go()` (community_screens.dart:1401) ile gelindiğinde yığın değişir, pop edilecek route kalmaz; `push` ile gelindiğinde sorun yok → aralıklı görünen "config hatası". ÇÖZÜM: `_afterAuthSuccess()` (`canPop()?pop():go('/home')`), hem e-posta hem Google girişine uygulandı. **METODOLOJİK DERS:** önceki oturum aynı semptomu "DEVELOPER_ERROR + Play Services yayılma gecikmesi" diye teşhis etmişti — ama o logcat dış-config düzeltilmeden önceki denemedendi; "yayılma gecikmesi" doğrulanamayan zayıf bir hipotezdi. Doğru hamle: semptomu (UI hata metni) değil, BAŞARI/başarısızlık sinyalini logcat'te izlemek. Genel `catch`'ler navigasyon/yan-etki hatalarını "auth başarısız" diye maskeleyebilir → başarı sonrası navigasyonu try-catch'in auth hatası dalından ayır. Regresyon: `test/auth_navigation_test.dart`.
* **[2026-06-25 — Onboarding "aha" çevrimdışı küratörlü]:** Onboarding 3→4 adım; `_AhaStep` ilgi alanına göre ilk ayeti `_firstAyahFor` switch'inden çeker (5 ilgi → 5 küratörlü Diyanet meali ayet, AyetFrame RTL/Amiri). DB/ağ bağımlılığı YOK (onboarding'de seed/Supabase init garantisi olmadığı için bilinçli — offline-first ilkesiyle tutarlı). CTA "İlk videonu oluştur" → `_complete('/studio')` (kurulumu bitirip stüdyoya).
* **[2026-07-15 — Video linkinden ayet tanıma NEDEN yapılmadı (mağaza + teknik gerekçe)]:** Ham istek "kullanıcı Instagram/TikTok/YouTube linki yapıştırır, sunucu videoyu İNDİRİR" idi. UYGULANMADI çünkü: (1) **App Store Review Guideline 5.2.3** birebir yasaklıyor — *"...include the ability to save, convert, or download media from third-party sources (e.g. Apple Music, YouTube, SoundCloud, Vimeo, etc.) without explicit authorization"* → tüm uygulamanın yayını reddedilir; Google Play'de de YouTube ToS ihlaliyle KALDIRILAN uygulama örnekleri belgeli. (2) **Teknik olarak da ölü:** yt-dlp datacenter IP'lerinde "Sign in to confirm you're not a bot" duvarına takılıyor, Supabase Edge Functions tam datacenter IP'sinden çıkar; 3. parti çözümleyici API'ler sürekli kırılır + maliyetli. **YERİNE:** video kullanıcının CİHAZINDAN gelir (galeri + paylaş menüsü) → uygulama hiçbir şey indirmez → 5.2.3 kapsamı dışı. Mevcut *link→og:image OCR* yolu (metinli reels için) aynen korundu; yalnız "sunucu videoyu indirsin" ayağı elendi. Ayrıca "whisper-quran-v1" HOSTED model YOK (uydurma isim); FFmpeg EF'de imkânsız+gereksiz (Groq mp4'ü doğrudan yutar, `url` param'ıyla bayt EF'den geçmez). Kapsam ≤3dk/≤20MB (Groq free 25MB altı). Detay: `~/.claude/plans/kur-an-daki-mesaj-video-staged-bumblebee.md`.
* **[2026-07-15 — Uthmani↔modern imla normalizasyonu (ASR köprüsü)]:** Korpus (`quran_data.ts`) uthmani hatta (`ٱلصَّلَوٰة`→harekesiz `الصلوه`), Whisper ise MODERN imla üretir (`الصلاة`→`الصلاه`) → aynı kelime farklı iskeletle token olarak HİÇ eşleşmiyordu. `normalizeArabic`'e küçük kural kümesi eklendi (`صلوة→صلاه`, `زكوة→زكاه`, `حيوة→حياه`, `الربوا→الربا` vb.) — her iki tarafa uygulandığı için OCR (uthmani görsel) yolu da bozulmaz. **AYRICA** `matcher.test.ts`'teki "gürültülü metin Bakara 153 bulur" testi HATALIYDI: sorgu `واستعينوا...` (vav'lı) aslında Bakara **45**'in birebir başlangıcı (153'te aynı kelime vav'sız); matcher baştan beri doğruydu, test yanlış ayet bekliyordu → düzeltildi + ayrı 153 testi eklendi. Deno testi bu sprint'e kadar hiç çalıştırılmamıştı (memory'de "elle doğrulandı" yazıyordu). `deno` 2.9.2 kuruldu.

## [7. KULLANILAN SKILL'LER]
* **9 maddelik plan turu (2026-08-03/04)** — eşleme `SKILL.md`'de. Fiilen çalıştırılanlar:
  `Explore` ×3 (dokunmadan önce çağıran taraması), `error-detective` ×2 (kök neden — İKİSİ de planın
  mekanizma açıklamasını düzeltti), `ecc:tdd-guide` ×4 (kırmızı-önce testler), `ecc:code-explorer`
  (caption veri yolu), `ecc:docs-lookup` (just_audio_background API, Context7), `ecc:database-reviewer`
  (Faz C politika/trigger denetimi — `user_blocks` RLS körlüğünü buldu), `run` skill (cihazda açma).
  **Ders:** kök neden ajanlarının raporu, onaylanmış planın çözüm önerisini üç yerde çürüttü —
  ajanları "planı doğrula" değil "gerçeği bul" diye görevlendirmek belirleyici oldu.
* **flutter-apply-architecture-best-practices** — katmanlı UI/Domain/Data yapısı, Repository+Service+ViewModel sözleşmesi.
* **senior-architect** — sistem tasarımı, mimari sentez, klasör yapısı.
* **senior-backend** — Supabase şema + RLS + Edge Functions + Storage tasarımı.
* **senior-frontend** — widget kompozisyonu, performans, modern Flutter API (withValues, CardThemeData, Riverpod 3 Notifier).
* **senior-security** — RLS default-deny, anon vs service_role, LLM anahtarı Edge'de, KVKK, ödeme doğrulaması.
* **design-taste-frontend / ui-ux-pro-max** — görsel polish (tasarım HTML+mockup'tan kilitli; ince ayar için).
* **office-hours** — başlangıç plan/keşif oturumu; ayrıca render worker erteleme stratejisi (wedge önce).
* **plan-design-review** — tüm planın 7-boyutlu tasarımcı incelemesi (koda çapraz-kontrol); 3 onaylı karar uygulandı.
* **superpowers:brainstorming** — Sesli Ayet Bulucu (video→tilavet) planı: ham plan Fable-ile-yeniden-kurgu isteği, 2 alt-ajanlı kanıtlı fizibilite (Groq/Whisper limitleri + App Store 5.2.3 + yt-dlp bot-duvarı), kapsam/giriş-yolu kararları AskUserQuestion ile alındı.
* **Animasyon yaklaşımı:** flutter_animate + Hero + implicit + CustomPainter + rive + lottie (mevcut Flutter animasyon yeteneği yeterli; ayrı skill gerekmedi).
